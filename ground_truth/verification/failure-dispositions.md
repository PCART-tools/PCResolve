# PCResolve 1.0.5 Failure Dispositions

This report classifies every current locked primary ownership mismatch. The canonical call labels remain in `ground_truth/calls/`; the JSONL sidecar records release disposition only.

## Classification Policy

Ground truth records semantic or runtime ownership. Release disposition asks a different question: whether project source proves that exact owner under PCResolve's project-source-only static analysis contract.

1. Verification levels describe GT evidence, not source recoverability. In particular, `dynamic_probe` and `manual_reasoned` are not automatic static-boundary waivers.
2. A runtime-only owner with a current `unknown` result is accepted as an honest static boundary and remains a scored GT miss.
3. A runtime-only owner with a current `local` or library result must first drop that unsupported certainty to `unknown`.
4. A still-unbound parameter in a function with no project source reference is considered only after a complete parse and declaration review. Defaults, annotations, decorators, implicit protocols, aliases and dispatch-table references prevent an automatic boundary waiver.
5. No library-name or external return-type whitelist is introduced to turn runtime observations into static guesses.
6. Value-type, attribute and dispatch dependencies are triage hints, not proofs of irrecoverability. They remain release blockers until bindings are traced or an independent boundary is reviewed.

## Release Disposition

| Disposition | Records | Meaning |
|---|---:|---|
| `fix_1_0_5` | 0 | Must be closed in 1.0.5 |
| `accepted_unknown` | 240 | Current unknown is justified by the static evidence boundary |
| `ground_truth_correction` | 0 | Canonical GT label must be corrected |
| **Total** | **240** | |

## Repair Scope

| Repair scope | Records |
|---|---:|
| `same_scope_result_protocol` | 0 |
| `bounded_receiver_flow` | 0 |
| `conservative_identity` | 0 |
| `local_identity` | 0 |
| `evidence_limited_unknown` | 240 |
| `label_correction` | 0 |
| **Total** | **240** |

## Evidence Boundary

| Verification level | Disposition | Records |
|---|---|---:|
| `dynamic_probe` | `accepted_unknown` | 62 |
| `manual_reasoned` | `accepted_unknown` | 3 |
| `static_context` | `accepted_unknown` | 175 |

## Source Recoverability

This table includes both boundary evidence and pending triage hints. Only the disposition and its justification determine whether a record is accepted for release.

| Source evidence | Records |
|---|---:|
| `bounded_or_pending` | 20 |
| `dynamic_callable_argument_requires_dispatch_evidence` | 3 |
| `instance_attribute_owner_requires_value_type_evidence` | 6 |
| `local_method_argument_requires_receiver_dispatch_evidence` | 4 |
| `parameter_attribute_item_requires_external_contract` | 2 |
| `receiver_owner_requires_value_type_evidence` | 178 |
| `unbound_parameter_without_project_reference` | 27 |

## Independently Reviewed Static Boundaries

These records remain raw GT misses. Each review is tied to an exact call identity and a digest of every Python source in its project; source or GT drift invalidates the release audit.

| Review | Active records |
|---|---:|
| `aibo-box2d-monkey-patched-shape-dispatch` | 1 |
| `aibo-external-optimizer-objective-values` | 1 |
| `aibo-geometry-external-spline-points` | 2 |
| `aibo-spline-noisy-parameter-return` | 1 |
| `aibo-uncalled-locomotion-objectives` | 2 |
| `aibo-uncalled-obstacle-helper-parameter` | 2 |
| `allnews-expanded-parameter-values` | 1 |
| `allnews-expanded-title-normalization` | 4 |
| `allnews-external-file-stream-values` | 5 |
| `allnews-html-escape-output-items` | 5 |
| `allnews-optional-invoke-mapping-argument` | 4 |
| `allnews-recursive-parser-argument-boundary` | 22 |
| `allnews-unestablished-page-title-value` | 4 |
| `allnews-unreferenced-function-parameters` | 3 |
| `contrucao-beautifulsoup-call-iteration` | 1 |
| `django-tornado-coroutine-payload` | 2 |
| `django-tornado-stream-callback` | 4 |
| `django-unreferenced-log-message-parameter` | 1 |
| `final-pandas-attribute-and-operation-results` | 20 |
| `final-pandas-column-label-element` | 1 |
| `final-uncalled-rmse-dataframe-parameters` | 4 |
| `flask2-framework-request-json-mapping` | 4 |
| `greenbenchmark-android-helper-parameters` | 6 |
| `greenbenchmark-pandas-plot-axis-return` | 11 |
| `greenbenchmark-pandas-produced-element-values` | 3 |
| `greenbenchmark-pandas-runtime-value-semantics` | 11 |
| `hfhd-external-stacked-value-items` | 2 |
| `hfhd-pandas-parameter-and-external-results` | 18 |
| `hfhd-simulation-external-array-results` | 2 |
| `mahe-albumentations-image-value` | 2 |
| `mahe-path-result-dictionary-key` | 1 |
| `mahe-tensor-input-arithmetic` | 12 |
| `mahe-unbound-model-output-and-gradient` | 10 |
| `mahe-uncalled-decimate-tensor-parameter` | 4 |
| `polire-custom-estimator-prediction-result` | 1 |
| `polire-fit-input-external-array-values` | 4 |
| `polire-polymorphic-grid-return-boundary` | 1 |
| `polire-uncalled-nsgp-input-and-kernel-results` | 7 |
| `polire-uncalled-spatial-features-training-value` | 1 |
| `polire-unreferenced-gridding-parameters` | 6 |
| `political-external-text-elements` | 15 |
| `political-pandas-accessor-results` | 5 |
| `political-split-element` | 1 |
| `python-workshop-polynomial-numeric-result` | 2 |
| `scrapping-beautifulsoup-attribute-value` | 8 |
| `simulation-porepy-grid-object` | 4 |
| `simulation-porepy-tag-value` | 2 |
| `tensorflow-external-base-config-result` | 1 |
| `youtube-sparse-distance-return-boundary` | 2 |
| `youtube-sparse-iteration-elements` | 4 |

## Project Queue

| Project | Records |
|---|---:|
| `allnews` | 48 |
| `greenbenchmark` | 31 |
| `MAHE_OD_DATASET` | 29 |
| `final` | 25 |
| `hfhd` | 22 |
| `political-polarisation` | 21 |
| `polire` | 20 |
| `AIBO` | 9 |
| `scrapping` | 8 |
| `django` | 7 |
| `Youtube` | 6 |
| `simulation` | 6 |
| `flask2` | 4 |
| `Python-Workshop` | 2 |
| `Contrucao` | 1 |
| `tensorflow1` | 1 |

## Failure Families

| Category | Records |
|---|---:|
| `transitive_method` | 54 |
| `python_protocol_method` | 44 |
| `numpy_array_receiver` | 26 |
| `torch_tensor_receiver` | 26 |
| `builtin_string_method` | 19 |
| `matplotlib_receiver` | 11 |
| `pandas_receiver` | 11 |
| `conversion_boundary` | 9 |
| `library_result_boundary` | 9 |
| `android_viewclient_receiver` | 6 |
| `pandas_receiver_chain` | 5 |
| `builtin_method_local_receiver` | 4 |
| `mapping_protocol_method` | 4 |
| `builtin_container_method` | 3 |
| `box2d_receiver` | 2 |
| `builtin` | 2 |
| `numpy_result_receiver` | 2 |
| `direct_import` | 1 |
| `keras_receiver` | 1 |
| `monkey_patched_local_method` | 1 |

## Unknown Outcome Queue

These records either already have a justified `unknown` result or must drop a source-unsupported `local`/library claim to `unknown`.

| Project | Category | Disposition | Records |
|---|---|---|---:|
| `AIBO` | `box2d_receiver` | `accepted_unknown` | 2 |
| `AIBO` | `monkey_patched_local_method` | `accepted_unknown` | 1 |
| `AIBO` | `numpy_array_receiver` | `accepted_unknown` | 6 |
| `Contrucao` | `library_result_boundary` | `accepted_unknown` | 1 |
| `MAHE_OD_DATASET` | `builtin_string_method` | `accepted_unknown` | 1 |
| `MAHE_OD_DATASET` | `numpy_array_receiver` | `accepted_unknown` | 2 |
| `MAHE_OD_DATASET` | `torch_tensor_receiver` | `accepted_unknown` | 26 |
| `Python-Workshop` | `transitive_method` | `accepted_unknown` | 2 |
| `Youtube` | `transitive_method` | `accepted_unknown` | 6 |
| `allnews` | `builtin` | `accepted_unknown` | 1 |
| `allnews` | `builtin_string_method` | `accepted_unknown` | 2 |
| `allnews` | `keras_receiver` | `accepted_unknown` | 1 |
| `allnews` | `python_protocol_method` | `accepted_unknown` | 44 |
| `django` | `builtin_method_local_receiver` | `accepted_unknown` | 3 |
| `django` | `transitive_method` | `accepted_unknown` | 4 |
| `final` | `builtin_method_local_receiver` | `accepted_unknown` | 1 |
| `final` | `direct_import` | `accepted_unknown` | 1 |
| `final` | `transitive_method` | `accepted_unknown` | 23 |
| `flask2` | `mapping_protocol_method` | `accepted_unknown` | 4 |
| `greenbenchmark` | `android_viewclient_receiver` | `accepted_unknown` | 6 |
| `greenbenchmark` | `builtin_container_method` | `accepted_unknown` | 3 |
| `greenbenchmark` | `matplotlib_receiver` | `accepted_unknown` | 11 |
| `greenbenchmark` | `pandas_receiver` | `accepted_unknown` | 11 |
| `hfhd` | `conversion_boundary` | `accepted_unknown` | 7 |
| `hfhd` | `transitive_method` | `accepted_unknown` | 15 |
| `polire` | `numpy_array_receiver` | `accepted_unknown` | 18 |
| `polire` | `numpy_result_receiver` | `accepted_unknown` | 2 |
| `political-polarisation` | `builtin_string_method` | `accepted_unknown` | 16 |
| `political-polarisation` | `pandas_receiver_chain` | `accepted_unknown` | 5 |
| `scrapping` | `library_result_boundary` | `accepted_unknown` | 8 |
| `simulation` | `conversion_boundary` | `accepted_unknown` | 2 |
| `simulation` | `transitive_method` | `accepted_unknown` | 4 |
| `tensorflow1` | `builtin` | `accepted_unknown` | 1 |

## Ground Truth Corrections

| Location | Expression | Reason |
|---|---|---|
| - | - | None in the current locked GT |

## Accepted Unknown Details

Every accepted static boundary is listed below. Ground truth names the reviewed semantic or runtime owner; the current result remains `unknown` because project source does not prove that owner.

### AIBO (9)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 1 | `functions/mujoco.py:40:12` | `x.reshape(self.policy_shape)` | `library / numpy` | `static_context` | `dynamic_callable_argument_requires_dispatch_evidence` | review aibo-uncalled-locomotion-objectives: Hopper and Ant are defined in functions/mujoco.py but have no project instantiation or objective call site. Their __call__(x) parameters have no default or concrete value binding. ndim, length and np.all checks describe an array protocol, not the class implementing x.reshape. |
| 2 | `functions/mujoco.py:115:12` | `x.reshape(self.policy_shape)` | `library / numpy` | `static_context` | `dynamic_callable_argument_requires_dispatch_evidence` | review aibo-external-optimizer-objective-values: run.py constructs HalfCheetah and passes it as f to optimizer implementations outside this fixture, including unavailable AIBO/TuRBO code and scipy/nevergrad. Direct objective invocations also obtain their inputs from external sampling or optimizer results. The project defines the objective but not the concrete type supplied for x; shape assertions alone do not identify the reshape descriptor owner. |
| 3 | `functions/mujoco.py:207:12` | `x.reshape(self.policy_shape)` | `library / numpy` | `static_context` | `dynamic_callable_argument_requires_dispatch_evidence` | review aibo-uncalled-locomotion-objectives: Hopper and Ant are defined in functions/mujoco.py but have no project instantiation or objective call site. Their __call__(x) parameters have no default or concrete value binding. ndim, length and np.all checks describe an array protocol, not the class implementing x.reshape. |
| 4 | `functions/push_utils.py:52:16` | `fixture.shape.draw(body, fixture)` | `local / local` | `static_context` | `receiver_owner_requires_value_type_evidence` | review aibo-box2d-monkey-patched-shape-dispatch: guiWorld.draw assigns local callables to b2PolygonShape.draw and b2CircleShape.draw, but body, fixture and fixture.shape enter through Box2D's external bodies and fixtures protocols. Project source does not prove that every runtime shape is one of those patched classes; the method name and the existence of two monkey patches do not select one local callable. |
| 5 | `functions/push_utils.py:198:14` | `world.CreateStaticBody(position=(0, 0), shapes=b2PolygonShape(vertices=verts))` | `library / Box2D` | `static_context` | `receiver_owner_requires_value_type_evidence` | review aibo-uncalled-obstacle-helper-parameter: add_obstacles is defined but never referenced or called by project source. Its b2world_interface parameter has no default, annotation or local binding, and world is read from that parameter's attribute. Other helpers receive the local b2WorldInterface class, but no such binding is supplied to add_obstacles; the matching attribute and method names are insufficient evidence. |
| 6 | `functions/push_utils.py:211:14` | `world.CreateStaticBody(position=pos, shapes=b2PolygonShape(box=blen))` | `library / Box2D` | `static_context` | `receiver_owner_requires_value_type_evidence` | review aibo-uncalled-obstacle-helper-parameter: add_obstacles is defined but never referenced or called by project source. Its b2world_interface parameter has no default, annotation or local binding, and world is read from that parameter's attribute. Other helpers receive the local b2WorldInterface class, but no such binding is supplied to add_obstacles; the matching attribute and method names are insufficient evidence. |
| 7 | `functions/rover_utils.py:44:17` | `params.reshape((-1, self.d))` | `library / numpy` | `static_context` | `local_method_argument_requires_receiver_dispatch_evidence` | review aibo-spline-noisy-parameter-return: PointBSpline.set_params receives params through RoverDomain.set_params as params + self.rnd_stream.normal(...). The optimizer argument and RandomState.normal result have no concrete type definition in this project. The other main() call passes start=np.zeros(2)+0.1 in this parameter position, not p.flatten(); zeros likewise has no result contract here. Constructor/call edges establish the path but cannot prove the resulting object's reshape owner. |
| 8 | `functions/rover_utils.py:164:16` | `lX.all(axis=1)` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review aibo-geometry-external-spline-points: AABoxes.contains receives trajectory points through ConstObstacleCost/NegGeom/UnionGeom. PointBSpline.get_points returns np.vstack(si.splev(...)).T; AABoxes bounds are also external array-construction results. Project code establishes neither the vstack result type nor the comparison-result protocol. X.ndim == 1 is a dimensionality check, not a class guard. Thus lX/hX cannot be proven ndarray from project source without an external result contract. |
| 9 | `functions/rover_utils.py:164:33` | `hX.all(axis=1)` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review aibo-geometry-external-spline-points: AABoxes.contains receives trajectory points through ConstObstacleCost/NegGeom/UnionGeom. PointBSpline.get_points returns np.vstack(si.splev(...)).T; AABoxes bounds are also external array-construction results. Project code establishes neither the vstack result type nor the comparison-result protocol. X.ndim == 1 is a dimensionality check, not a class guard. Thus lX/hX cannot be proven ndarray from project source without an external result contract. |

### Contrucao (1)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 10 | `main.py:10:8` | `data.decompose()` | `library / bs4` | `manual_reasoned` | `receiver_owner_requires_value_type_evidence` | review contrucao-beautifulsoup-call-iteration: soup is constructed with BeautifulSoup at the call site, but data is an element yielded by calling that external object. The project defines neither BeautifulSoup.__call__ nor the iterable result element type; assigning bs4 from the producer owner alone would conflate callable and result ownership. |

### MAHE_OD_DATASET (29)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 11 | `annotation.py:63:8` | `key.split('/')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-path-result-dictionary-key: annotation.py binds path to os.path.abspath(...), builds image_path = path + '/' + image_name and inserts it as a dictionary key. Literal-key and write/alias tracking is now covered by regression tests, but abspath has no result-type contract in this analyzer or implementation in the project. Inferring key.split as builtin str from the producer's module name would require a new external result contract. |
| 12 | `preprocess.py:146:10` | `image.copy()` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-albumentations-image-value: image is supplied from the image item of an external Albumentations transform result; project source does not define that item's concrete NumPy type |
| 13 | `rescaling.py:88:10` | `image.copy()` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-albumentations-image-value: image is supplied from the image item of an external Albumentations transform result; project source does not define that item's concrete NumPy type |
| 14 | `utils.py:166:11` | `tensor.dim()` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-uncalled-decimate-tensor-parameter: decimate is defined but never referenced by project source. Its tensor parameter has no default value or type annotation. The docstring states the intended tensor protocol, but no call edge supplies a concrete object, so dim, index_select and size cannot be assigned to Torch without treating documentation or method names as runtime type evidence. |
| 15 | `utils.py:167:19` | `tensor.dim()` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-uncalled-decimate-tensor-parameter: decimate is defined but never referenced by project source. Its tensor parameter has no default value or type annotation. The docstring states the intended tensor protocol, but no call edge supplies a concrete object, so dim, index_select and size cannot be assigned to Torch without treating documentation or method names as runtime type evidence. |
| 16 | `utils.py:169:21` | `tensor.index_select(dim=d, index=torch.arange(start=0, end=tensor.size(d), step=m[d]).long())` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-uncalled-decimate-tensor-parameter: decimate is defined but never referenced by project source. Its tensor parameter has no default value or type annotation. The docstring states the intended tensor protocol, but no call edge supplies a concrete object, so dim, index_select and size cannot be assigned to Torch without treating documentation or method names as runtime type evidence. |
| 17 | `utils.py:170:73` | `tensor.size(d)` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-uncalled-decimate-tensor-parameter: decimate is defined but never referenced by project source. Its tensor parameter has no default value or type annotation. The docstring states the intended tensor protocol, but no call edge supplies a concrete object, so dim, index_select and size cannot be assigned to Torch without treating documentation or method names as runtime type evidence. |
| 18 | `utils.py:197:33` | `true_labels[i].size(0)` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 19 | `utils.py:209:32` | `det_labels[i].size(0)` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 20 | `utils.py:261:41` | `overlaps.squeeze(0)` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 21 | `utils.py:295:15` | `recalls_above_t.any()` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 22 | `utils.py:380:56` | `set_2[:, :2].unsqueeze(0)` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 23 | `utils.py:381:56` | `set_2[:, 2:].unsqueeze(0)` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 24 | `utils.py:404:12` | `areas_set_1.unsqueeze(1)` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 25 | `utils.py:404:39` | `areas_set_2.unsqueeze(0)` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 26 | `utils.py:503:22` | `overlap.squeeze(0)` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 27 | `utils.py:506:15` | `overlap.max()` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 28 | `utils.py:506:15` | `overlap.max().item()` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 29 | `utils.py:520:19` | `centers_in_crop.any()` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-tensor-input-arithmetic: find_intersection/find_jaccard_overlap receive boxes from calculate_mAP parameters and random_crop inputs. Slice, arithmetic and comparison results then become method receivers. calculate_mAP has no project call site, and the augmentation path does not supply a source-defined tensor result/element implementation. Docstrings identify intended tensors but do not exclude other protocol implementations at these parameter-dependent sites. |
| 30 | `utils.py:692:17` | `targets.size(0)` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |
| 31 | `utils.py:693:13` | `scores.topk(k, 1, True, True)` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |
| 32 | `utils.py:694:14` | `ind.eq(targets.view(-1, 1).expand_as(ind))` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |
| 33 | `utils.py:694:21` | `targets.view(-1, 1)` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |
| 34 | `utils.py:694:21` | `targets.view(-1, 1).expand_as(ind)` | `library / torch` | `static_context` | `unbound_parameter_without_project_reference` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |
| 35 | `utils.py:695:20` | `correct.view(-1)` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |
| 36 | `utils.py:695:20` | `correct.view(-1).float()` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |
| 37 | `utils.py:695:20` | `correct.view(-1).float().sum()` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |
| 38 | `utils.py:696:11` | `correct_total.item()` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |
| 39 | `utils.py:752:16` | `param.grad.data.clamp_(-grad_clip, grad_clip)` | `library / torch` | `static_context` | `receiver_owner_requires_value_type_evidence` | review mahe-unbound-model-output-and-gradient: accuracy and clip_gradient have no project callers. ind is a tuple element returned by scores.topk; later receivers are its method results. param is obtained through optimizer.param_groups and group['params'], and its grad.data attribute type is external. Neither the input objects nor their result/attribute types are defined in project source. |

### Python-Workshop (2)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 40 | `myregr.py:16:11` | `res.dot(res)` | `library / numpy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review python-workshop-polynomial-numeric-result: res and dif depend on the result of calling a NumPy poly1d instance and arithmetic with y. y is returned through normalize using external mean/std operations, while polynomial coefficients reach callbacks through scipy.optimize.minimize. Existing dynamic probes establish the runtime ndarray owner but project source has no poly1d result or optimizer callback value contract. |
| 41 | `myregr.py:24:17` | `dif.dot(xp)` | `library / numpy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review python-workshop-polynomial-numeric-result: res and dif depend on the result of calling a NumPy poly1d instance and arithmetic with y. y is returned through normalize using external mean/std operations, while polynomial coefficients reach callbacks through scipy.optimize.minimize. Existing dynamic probes establish the runtime ndarray owner but project source has no poly1d result or optimizer callback value contract. |

### Youtube (6)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 42 | `Kmeans.py:39:15` | `D.argmin(axis=1)` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review youtube-sparse-distance-return-boundary: kmeans assigns D from cdist_sparse and selects distances from D. cdist_sparse has a verified cdist-return branch, but its other normal return path constructs d using np.empty and returns d or 1-d. np.empty has no result contract or implementation in project scope. Its dense non-cosine branch additionally reads d before assignment and can raise; that erroneous path is not evidence of a concrete result. The verified cdist branch cannot establish the owner of all other return paths. |
| 43 | `Kmeans.py:41:17` | `distances.mean()` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review youtube-sparse-distance-return-boundary: kmeans assigns D from cdist_sparse and selects distances from D. cdist_sparse has a verified cdist-return branch, but its other normal return path constructs d using np.empty and returns d or 1-d. np.empty has no result contract or implementation in project scope. Its dense non-cosine branch additionally reads d before assignment and can raise; that erroneous path is not evidence of a concrete result. The verified cdist branch cannot establish the owner of all other return paths. |
| 44 | `Kmeans.py:67:26` | `x.todense()` | `library / scipy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review youtube-sparse-iteration-elements: x and y are elements iterated from cdist_sparse parameters X and Y. The sparse branches are selected using scipy.sparse.issparse, whose predicate and iterator element semantics are external. The runtime probe supports SciPy ownership but the project does not define a sparse element type; an iterated element must not inherit its container library without that contract. |
| 45 | `Kmeans.py:70:31` | `y.todense()` | `library / scipy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review youtube-sparse-iteration-elements: x and y are elements iterated from cdist_sparse parameters X and Y. The sparse branches are selected using scipy.sparse.issparse, whose predicate and iterator element semantics are external. The runtime probe supports SciPy ownership but the project does not define a sparse element type; an iterated element must not inherit its container library without that contract. |
| 46 | `Kmeans.py:74:32` | `x.todense()` | `library / scipy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review youtube-sparse-iteration-elements: x and y are elements iterated from cdist_sparse parameters X and Y. The sparse branches are selected using scipy.sparse.issparse, whose predicate and iterator element semantics are external. The runtime probe supports SciPy ownership but the project does not define a sparse element type; an iterated element must not inherit its container library without that contract. |
| 47 | `Kmeans.py:74:45` | `y.todense()` | `library / scipy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review youtube-sparse-iteration-elements: x and y are elements iterated from cdist_sparse parameters X and Y. The sparse branches are selected using scipy.sparse.issparse, whose predicate and iterator element semantics are external. The runtime probe supports SciPy ownership but the project does not define a sparse element type; an iterated element must not inherit its container library without that contract. |

### allnews (48)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 48 | `allnews_am/NER_models/ner.py:112:12` | `model.fit(X_tr, np.array(y_tr), batch_size=BATCH_SIZE, epochs=EPOCHS, validation_split=0.1, validation_data=(X_te, np.array(y_te)), verbose=2)` | `library / keras` | `static_context` | `unbound_parameter_without_project_reference` | review allnews-unreferenced-function-parameters: training_model, tokenize, and normalizeTitle have no executable project-source call sites. Their receiver parameters have no defaults or type annotations; normalizeTitle's only apparent caller is commented out. Docstrings and the methods used inside each body describe intended behavior but do not establish the runtime owner of an otherwise unbound parameter. Therefore model.fit, s.strip, and title.strip remain unknown under project-source-only static analysis. |
| 49 | `allnews_am/processing.py:84:8` | `s.strip()` | `python / python` | `static_context` | `unbound_parameter_without_project_reference` | review allnews-unreferenced-function-parameters: training_model, tokenize, and normalizeTitle have no executable project-source call sites. Their receiver parameters have no defaults or type annotations; normalizeTitle's only apparent caller is commented out. Docstrings and the methods used inside each body describe intended behavior but do not establish the runtime owner of an otherwise unbound parameter. Therefore model.fit, s.strip, and title.strip remain unknown under project-source-only static analysis. |
| 50 | `allnews_am/wikiextractor/WikiExtractor.py:289:12` | `title.strip(' _')` | `python / python` | `static_context` | `unbound_parameter_without_project_reference` | review allnews-unreferenced-function-parameters: training_model, tokenize, and normalizeTitle have no executable project-source call sites. Their receiver parameters have no defaults or type annotations; normalizeTitle's only apparent caller is commented out. Docstrings and the methods used inside each body describe intended behavior but do not establish the runtime owner of an otherwise unbound parameter. Therefore model.fit, s.strip, and title.strip remain unknown under project-source-only static analysis. |
| 51 | `allnews_am/wikiextractor/WikiExtractor.py:592:27` | `line.encode('utf-8')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-html-escape-output-items: Extractor.clean has an options.toHTML branch that returns cgi.escape(text). extract passes the clean result to compact, which calls split and then string methods on its pieces; write_output subsequently encodes the compacted lines (and a title-derived header). cgi.escape has no return-type contract in this analyzer or implementation in the project. The non-HTML branch and literal headers do not prove the type of every item from the HTML branch. |
| 52 | `allnews_am/wikiextractor/WikiExtractor.py:609:16` | `self.title.find(':')` | `python / python` | `static_context` | `instance_attribute_owner_requires_value_type_evidence` | review allnews-unestablished-page-title-value: pages_from initializes title=None and sets it from input matches, but its yield condition does not require that a title was encountered. load_templates reads that yielded title, and Extractor also receives a title through jobs_queue.get()/tuple forwarding. Thus self.title and its pagename aliases do not have a uniformly established string binding. A malformed page or an opaque queued job is a concrete unproven path; another string-producing path cannot establish the owner for all calls. |
| 53 | `allnews_am/wikiextractor/WikiExtractor.py:620:16` | `pagename.rfind('/')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-unestablished-page-title-value: pages_from initializes title=None and sets it from input matches, but its yield condition does not require that a title was encountered. load_templates reads that yielded title, and Extractor also receives a title through jobs_queue.get()/tuple forwarding. Thus self.title and its pagename aliases do not have a uniformly established string binding. A malformed page or an opaque queued job is a concrete unproven path; another string-producing path cannot establish the owner for all calls. |
| 54 | `allnews_am/wikiextractor/WikiExtractor.py:627:16` | `pagename.find('/')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-unestablished-page-title-value: pages_from initializes title=None and sets it from input matches, but its yield condition does not require that a title was encountered. load_templates reads that yielded title, and Extractor also receives a title through jobs_queue.get()/tuple forwarding. Thus self.title and its pagename aliases do not have a uniformly established string binding. A malformed page or an opaque queued job is a concrete unproven path; another string-producing path cannot establish the owner for all calls. |
| 55 | `allnews_am/wikiextractor/WikiExtractor.py:929:28` | `param.strip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-expanded-parameter-values: expandTemplate builds parameters with [self.transform(p) for p in parts[1:]] before calling templateParams, and the invoke branch does the same. transform -> transform1 -> expand -> expandTemplate can return self.magicWords[title]; extract populates that mapping with time.strftime results and a queue-derived page title. Those values have no project-defined concrete type or existing result contract. The expanded value is accumulated with overloaded +/+=, with no str() or join() conversion on this path. Consequently param.strip cannot be justified solely by the list comprehension; the regex-match branch establishes other values, not this unmatched branch. |
| 56 | `allnews_am/wikiextractor/WikiExtractor.py:988:16` | `parts[0].strip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 57 | `allnews_am/wikiextractor/WikiExtractor.py:1152:14` | `paramsList[cur:s].split(sep)` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 58 | `allnews_am/wikiextractor/WikiExtractor.py:1168:10` | `paramsList[cur:].split(sep)` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 59 | `allnews_am/wikiextractor/WikiExtractor.py:1397:16` | `args.get(var)` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-optional-invoke-mapping-argument: callParserFunction's invoke branch initializes params=None when len(args)==2, searches the frame chain, and calls sharp_invoke even if no matching frame was found. Frame.__init__ also defaults args to []. sharp_invoke dispatches that value to functionParams/roman_main. The dictionary returned by templateParams covers a different branch only; neither None nor the empty-list default establishes a dict receiver for these four get calls. |
| 60 | `allnews_am/wikiextractor/WikiExtractor.py:1399:20` | `args.get(str(index))` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-optional-invoke-mapping-argument: callParserFunction's invoke branch initializes params=None when len(args)==2, searches the frame chain, and calls sharp_invoke even if no matching frame was found. Frame.__init__ also defaults args to []. sharp_invoke dispatches that value to functionParams/roman_main. The dictionary returned by templateParams covers a different branch only; neither None nor the empty-list default establishes a dict receiver for these four get calls. |
| 61 | `allnews_am/wikiextractor/WikiExtractor.py:1487:20` | `args.get('1')` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-optional-invoke-mapping-argument: callParserFunction's invoke branch initializes params=None when len(args)==2, searches the frame chain, and calls sharp_invoke even if no matching frame was found. Frame.__init__ also defaults args to []. sharp_invoke dispatches that value to functionParams/roman_main. The dictionary returned by templateParams covers a different branch only; neither None nor the empty-list default establishes a dict receiver for these four get calls. |
| 62 | `allnews_am/wikiextractor/WikiExtractor.py:1491:15` | `args.get('2', 'N/A')` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-optional-invoke-mapping-argument: callParserFunction's invoke branch initializes params=None when len(args)==2, searches the frame chain, and calls sharp_invoke even if no matching frame was found. Frame.__init__ also defaults args to []. sharp_invoke dispatches that value to functionParams/roman_main. The dictionary returned by templateParams covers a different branch only; neither None nor the empty-list default establishes a dict receiver for these four get calls. |
| 63 | `allnews_am/wikiextractor/WikiExtractor.py:1674:15` | `string[0].upper()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-expanded-title-normalization: fullyQualifiedTemplateTitle is called with title after self.expand(title), and ucfirst/lcfirst also receive parserFunctions callback arguments. The expanded value can originate in magicWords (time.strftime results or the queue-derived title) and is concatenated without an explicit string conversion, then forwarded via parts[0]. Regex group and literal-title callers prove only their own paths. Truthiness, slicing, and length checks do not prove a string class for every incoming value. This is the same externally fed parser cycle, not evidence that every startswith/upper/lower receiver is Python-owned. |
| 64 | `allnews_am/wikiextractor/WikiExtractor.py:1683:19` | `string[0].lower()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-expanded-title-normalization: fullyQualifiedTemplateTitle is called with title after self.expand(title), and ucfirst/lcfirst also receive parserFunctions callback arguments. The expanded value can originate in magicWords (time.strftime results or the queue-derived title) and is concatenated without an explicit string conversion, then forwarded via parts[0]. Regex group and literal-title callers prove only their own paths. Truthiness, slicing, and length checks do not prove a string class for every incoming value. This is the same externally fed parser cycle, not evidence that every startswith/upper/lower receiver is Python-owned. |
| 65 | `allnews_am/wikiextractor/WikiExtractor.py:1685:19` | `string.lower()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-expanded-title-normalization: fullyQualifiedTemplateTitle is called with title after self.expand(title), and ucfirst/lcfirst also receive parserFunctions callback arguments. The expanded value can originate in magicWords (time.strftime results or the queue-derived title) and is concatenated without an explicit string conversion, then forwarded via parts[0]. Regex group and literal-title callers prove only their own paths. Truthiness, slicing, and length checks do not prove a string class for every incoming value. This is the same externally fed parser cycle, not evidence that every startswith/upper/lower receiver is Python-owned. |
| 66 | `allnews_am/wikiextractor/WikiExtractor.py:1695:7` | `templateTitle.startswith(':')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-expanded-title-normalization: fullyQualifiedTemplateTitle is called with title after self.expand(title), and ucfirst/lcfirst also receive parserFunctions callback arguments. The expanded value can originate in magicWords (time.strftime results or the queue-derived title) and is concatenated without an explicit string conversion, then forwarded via parts[0]. Regex group and literal-title callers prove only their own paths. Truthiness, slicing, and length checks do not prove a string class for every incoming value. This is the same externally fed parser cycle, not evidence that every startswith/upper/lower receiver is Python-owned. |
| 67 | `allnews_am/wikiextractor/WikiExtractor.py:1780:7` | `testValue.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 68 | `allnews_am/wikiextractor/WikiExtractor.py:1783:34` | `valueIfTrue.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 69 | `allnews_am/wikiextractor/WikiExtractor.py:1787:27` | `valueIfFalse.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 70 | `allnews_am/wikiextractor/WikiExtractor.py:1792:13` | `rvalue.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 71 | `allnews_am/wikiextractor/WikiExtractor.py:1795:11` | `lvalue.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 72 | `allnews_am/wikiextractor/WikiExtractor.py:1802:35` | `valueIfTrue.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 73 | `allnews_am/wikiextractor/WikiExtractor.py:1805:35` | `valueIfFalse.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 74 | `allnews_am/wikiextractor/WikiExtractor.py:1811:27` | `then.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 75 | `allnews_am/wikiextractor/WikiExtractor.py:1813:15` | `test.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 76 | `allnews_am/wikiextractor/WikiExtractor.py:1815:27` | `Else.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 77 | `allnews_am/wikiextractor/WikiExtractor.py:1829:14` | `primary.strip()` | `python / python` | `static_context` | `bounded_or_pending` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 78 | `allnews_am/wikiextractor/WikiExtractor.py:1837:15` | `param.split('=', 1)` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 79 | `allnews_am/wikiextractor/WikiExtractor.py:1838:29` | `pair[0].strip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 80 | `allnews_am/wikiextractor/WikiExtractor.py:1842:33` | `pair[1].strip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 81 | `allnews_am/wikiextractor/WikiExtractor.py:1900:51` | `string.encode('utf-8')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 82 | `allnews_am/wikiextractor/WikiExtractor.py:1902:38` | `string.lower()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 83 | `allnews_am/wikiextractor/WikiExtractor.py:1906:38` | `string.upper()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 84 | `allnews_am/wikiextractor/WikiExtractor.py:1929:26` | `args[0].strip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 85 | `allnews_am/wikiextractor/WikiExtractor.py:1929:43` | `args[1].strip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-recursive-parser-argument-boundary: The parser recursion has an external-value entry, not only an unresolved call edge. extract stores time.strftime results and the queue-derived title in magicWords; expandTemplate returns a selected value (1001-1004) and expand concatenates it without an explicit string conversion. expandTemplate assigns the expanded title tail to parts[0] (1022) and callParserFunction forwards *args through parserFunctions. sharp_expr passes that first argument back to extr.expand (1767), so splitParts/expandTemplate and the parser callbacks can receive the unproven value again. Trailing parts and sharp_switch slices inherit this recursive input. Initial ''.join(lines), Template.subst's join, literal defaults and known str-only branches do not prove all values in this cycle. No method-name inference or new strftime/quote return contract is used; concrete GT remains unchanged. |
| 86 | `allnews_am/wikiextractor/WikiExtractor.py:2538:16` | `text.split('\n')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-html-escape-output-items: Extractor.clean has an options.toHTML branch that returns cgi.escape(text). extract passes the clean result to compact, which calls split and then string methods on its pieces; write_output subsequently encodes the compacted lines (and a title-derived header). cgi.escape has no return-type contract in this analyzer or implementation in the project. The non-HTML branch and literal headers do not prove the type of every item from the HTML branch. |
| 87 | `allnews_am/wikiextractor/WikiExtractor.py:2571:13` | `line.startswith('++')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-html-escape-output-items: Extractor.clean has an options.toHTML branch that returns cgi.escape(text). extract passes the clean result to compact, which calls split and then string methods on its pieces; write_output subsequently encodes the compacted lines (and a title-derived header). cgi.escape has no return-type contract in this analyzer or implementation in the project. The non-HTML branch and literal headers do not prove the type of every item from the HTML branch. |
| 88 | `allnews_am/wikiextractor/WikiExtractor.py:2610:19` | `line[i:].strip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-html-escape-output-items: Extractor.clean has an options.toHTML branch that returns cgi.escape(text). extract passes the clean result to compact, which calls split and then string methods on its pieces; write_output subsequently encodes the compacted lines (and a title-derived header). cgi.escape has no return-type contract in this analyzer or implementation in the project. The non-HTML branch and literal headers do not prove the type of every item from the HTML branch. |
| 89 | `allnews_am/wikiextractor/WikiExtractor.py:2639:53` | `line.strip('.-')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-html-escape-output-items: Extractor.clean has an options.toHTML branch that returns cgi.escape(text). extract passes the clean result to compact, which calls split and then string methods on its pieces; write_output subsequently encodes the compacted lines (and a title-derived header). cgi.escape has no return-type contract in this analyzer or implementation in the project. The non-HTML branch and literal headers do not prove the type of every item from the HTML branch. |
| 90 | `allnews_am/wikiextractor/WikiExtractor.py:2758:24` | `title.find(':')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-unestablished-page-title-value: pages_from initializes title=None and sets it from input matches, but its yield condition does not require that a title was encountered. load_templates reads that yielded title, and Extractor also receives a title through jobs_queue.get()/tuple forwarding. Thus self.title and its pagename aliases do not have a uniformly established string binding. A malformed page or an opaque queued job is a concrete unproven path; another string-producing path cannot establish the owner for all calls. |
| 91 | `allnews_am/wikiextractor/WikiExtractor.py:2803:51` | `line.decode('utf-8')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-external-file-stream-values: line and page values enter through file, compression, JSON, or XML iterator protocols whose concrete yielded value types are not declared in project source |
| 92 | `allnews_am/wikiextractor/WikiExtractor.py:2808:19` | `line.lstrip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-external-file-stream-values: line and page values enter through file, compression, JSON, or XML iterator protocols whose concrete yielded value types are not declared in project source |
| 93 | `allnews_am/wikiextractor/WikiExtractor.py:2808:19` | `line.lstrip().startswith('[[Category:')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-external-file-stream-values: line and page values enter through file, compression, JSON, or XML iterator protocols whose concrete yielded value types are not declared in project source |
| 94 | `allnews_am/wikiextractor/WikiExtractor.py:2877:51` | `line.decode('utf-8')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-external-file-stream-values: line and page values enter through file, compression, JSON, or XML iterator protocols whose concrete yielded value types are not declared in project source |
| 95 | `allnews_am/wikiextractor/cirrus-extract.py:183:25` | `page.encode('utf-8')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review allnews-external-file-stream-values: line and page values enter through file, compression, JSON, or XML iterator protocols whose concrete yielded value types are not declared in project source |

### django (7)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 96 | `proxy.py:30:4` | `stream.read_until(delimiter, cb)` | `library / tornado` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review django-tornado-stream-callback: stream enters only through Tornado's external handle_stream callback; project source passes that value onward but does not define its class or its socket attribute type |
| 97 | `proxy.py:93:22` | `line.split()` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review django-tornado-coroutine-payload: line is produced by Tornado's external asynchronous read protocol; project source contains no return contract proving the payload or split element type |
| 98 | `proxy.py:97:25` | `obj[3].split('/')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review django-tornado-coroutine-payload: line is produced by Tornado's external asynchronous read protocol; project source contains no return contract proving the payload or split element type |
| 99 | `proxy.py:109:26` | `msg.format(*args, **kwargs)` | `python / python` | `dynamic_probe` | `unbound_parameter_without_project_reference` | review django-unreferenced-log-message-parameter: SimpleEcho.log has no executable project-source call site. Its only apparent self.log call is commented out, and msg has no default or type annotation. The commented string argument and the use of format inside the dead method show intent but cannot establish the runtime owner of msg from executable project source. |
| 100 | `proxy.py:129:8` | `stream.set_close_callback(conn.on_disconnect)` | `library / tornado` | `dynamic_probe` | `local_method_argument_requires_receiver_dispatch_evidence` | review django-tornado-stream-callback: stream enters only through Tornado's external handle_stream callback; project source passes that value onward but does not define its class or its socket attribute type |
| 101 | `proxy.py:130:8` | `stream.socket.setsockopt(socket.IPPROTO_TCP, socket.TCP_NODELAY, 1)` | `library / socket` | `dynamic_probe` | `local_method_argument_requires_receiver_dispatch_evidence` | review django-tornado-stream-callback: stream enters only through Tornado's external handle_stream callback; project source passes that value onward but does not define its class or its socket attribute type |
| 102 | `proxy.py:131:8` | `stream.socket.setsockopt(socket.IPPROTO_TCP, socket.SO_KEEPALIVE, 1)` | `library / socket` | `dynamic_probe` | `local_method_argument_requires_receiver_dispatch_evidence` | review django-tornado-stream-callback: stream enters only through Tornado's external handle_stream callback; project source passes that value onward but does not define its class or its socket attribute type |

### final (25)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 103 | `explore.py:71:31` | `y_train.colonies_lost.mean()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 104 | `explore.py:73:32` | `y_train.colonies_lost.median()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 105 | `explore.py:75:34` | `y_validate.colonies_lost.mean()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 106 | `explore.py:77:35` | `y_validate.colonies_lost.median()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 107 | `explore.py:223:4` | `y_test.colonies_lost.hist(bins=1000)` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 108 | `explore.py:241:31` | `y_train.colonies_lost.mean()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 109 | `explore.py:243:32` | `y_train.colonies_lost.median()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 110 | `explore.py:245:34` | `y_validate.colonies_lost.mean()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 111 | `explore.py:247:35` | `y_validate.colonies_lost.median()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 112 | `explore.py:292:4` | `train.colonies_lost.groupby([train.index.year, train.index.month])` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 113 | `explore.py:292:4` | `train.colonies_lost.groupby([train.index.year, train.index.month]).sum()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 114 | `explore.py:292:4` | `train.colonies_lost.groupby([train.index.year, train.index.month]).sum().unstack(0)` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 115 | `explore.py:292:4` | `train.colonies_lost.groupby([train.index.year, train.index.month]).sum().unstack(0).plot(figsize=(14, 10))` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 116 | `explore.py:294:15` | `train.colonies_lost.groupby(train.index.month)` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 117 | `explore.py:294:15` | `train.colonies_lost.groupby(train.index.month).sum()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 118 | `explore.py:297:4` | `mean_line.plot(label='Average', color='red', linewidth=4, legend=True)` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 119 | `final.py:372:56` | `col.endswith('scaled')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-column-label-element: col is iterated from a pandas columns object returned by an external DataFrame; project source does not provide an independent Python str element contract for that iterator |
| 120 | `regression_models.py:51:31` | `y_train.colonies_lost.mean()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 121 | `regression_models.py:53:32` | `y_train.colonies_lost.median()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 122 | `regression_models.py:55:34` | `y_validate.colonies_lost.mean()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 123 | `regression_models.py:57:35` | `y_validate.colonies_lost.median()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review final-pandas-attribute-and-operation-results: the project supplies pandas objects at several call sites, but each receiver crosses DataFrame attribute lookup, grouping, aggregation, arithmetic, or plotting result semantics implemented outside the project |
| 124 | `regression_models.py:78:31` | `y_train.colonies_lost.mean()` | `library / pandas` | `dynamic_probe` | `unbound_parameter_without_project_reference` | review final-uncalled-rmse-dataframe-parameters: get_RMSE has no project-source call site. The notebook calls the distinct get_baseline_RMSE function, so its pandas-backed arguments do not flow into get_RMSE. The y_train and y_validate parameters have no defaults or type annotations; column names and pandas method names express intent but do not prove their runtime owners. |
| 125 | `regression_models.py:80:32` | `y_train.colonies_lost.median()` | `library / pandas` | `dynamic_probe` | `unbound_parameter_without_project_reference` | review final-uncalled-rmse-dataframe-parameters: get_RMSE has no project-source call site. The notebook calls the distinct get_baseline_RMSE function, so its pandas-backed arguments do not flow into get_RMSE. The y_train and y_validate parameters have no defaults or type annotations; column names and pandas method names express intent but do not prove their runtime owners. |
| 126 | `regression_models.py:82:34` | `y_validate.colonies_lost.mean()` | `library / pandas` | `dynamic_probe` | `unbound_parameter_without_project_reference` | review final-uncalled-rmse-dataframe-parameters: get_RMSE has no project-source call site. The notebook calls the distinct get_baseline_RMSE function, so its pandas-backed arguments do not flow into get_RMSE. The y_train and y_validate parameters have no defaults or type annotations; column names and pandas method names express intent but do not prove their runtime owners. |
| 127 | `regression_models.py:84:35` | `y_validate.colonies_lost.median()` | `library / pandas` | `dynamic_probe` | `unbound_parameter_without_project_reference` | review final-uncalled-rmse-dataframe-parameters: get_RMSE has no project-source call site. The notebook calls the distinct get_baseline_RMSE function, so its pandas-backed arguments do not flow into get_RMSE. The y_train and y_validate parameters have no defaults or type annotations; column names and pandas method names express intent but do not prove their runtime owners. |

### flask2 (4)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 128 | `app.py:66:27` | `request.json.get('description', '')` | `python / python` | `static_context` | `bounded_or_pending` | request.json has mapping-like runtime behavior, but its concrete value type is not proven by project source |
| 129 | `app.py:83:24` | `request.json.get('title', task['title'])` | `python / python` | `static_context` | `bounded_or_pending` | request.json has mapping-like runtime behavior, but its concrete value type is not proven by project source |
| 130 | `app.py:84:30` | `request.json.get('description', task['description'])` | `python / python` | `static_context` | `bounded_or_pending` | request.json has mapping-like runtime behavior, but its concrete value type is not proven by project source |
| 131 | `app.py:85:23` | `request.json.get('done', task['done'])` | `python / python` | `static_context` | `bounded_or_pending` | request.json has mapping-like runtime behavior, but its concrete value type is not proven by project source |

### greenbenchmark (31)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 132 | `report_all_results.py:52:11` | `df_energy.loc[t1:t2, [power_feature, 'timestamp_delta']].product(axis=1)` | `library / pandas` | `static_context` | `parameter_attribute_item_requires_external_contract` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 133 | `report_all_results.py:52:11` | `df_energy.loc[t1:t2, [power_feature, 'timestamp_delta']].product(axis=1).sum()` | `library / pandas` | `static_context` | `parameter_attribute_item_requires_external_contract` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 134 | `report_all_results.py:79:11` | `sample1.var()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 135 | `report_all_results.py:80:11` | `sample2.var()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 136 | `report_all_results.py:81:12` | `sample2.mean()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 137 | `report_all_results.py:81:29` | `sample1.mean()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 138 | `report_all_results.py:97:29` | `group.mean()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 139 | `report_all_results.py:97:52` | `group.std()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 140 | `report_all_results.py:121:29` | `experiment.replace(experiment_pivot + '-lint-', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-produced-element-values: experiment is iterated from a list built from pandas unique output; list construction proves the container but project source does not declare the external result element type as str |
| 141 | `report_all_results.py:122:29` | `experiment.replace('-', ' ')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-produced-element-values: experiment is iterated from a list built from pandas unique output; list construction proves the container but project source does not declare the external result element type as str |
| 142 | `report_all_results.py:122:29` | `experiment.replace('-', ' ').title()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-produced-element-values: experiment is iterated from a list built from pandas unique output; list construction proves the container but project source does not declare the external result element type as str |
| 143 | `report_all_results.py:184:29` | `group[energy_feature].mean()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 144 | `report_all_results.py:185:28` | `group[energy_feature].std()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 145 | `report_all_results.py:192:33` | `group[energy_feature].mean()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-runtime-value-semantics: the project proves an outer pandas value but these receivers cross pandas indexing, grouping iteration, transform callback, or subscript-result boundaries whose concrete runtime result types are defined outside the project |
| 146 | `report_all_results.py:217:8` | `ax.axvline(timestamp, color='darkblue', linestyle='--', alpha=0.5, linewidth=2)` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 147 | `report_all_results.py:223:4` | `ax.annotate('t0 - InteractionStarted', xy=(df_event.loc['InteractionStarted'], arrowY), xytext=(df_event.loc['InteractionStarted'], textY), ha='left', arrowprops=arrowprops, bbox=bbox_props)` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 148 | `report_all_results.py:226:4` | `ax.annotate('tn - InteractionEnded', xy=(df_event.loc['InteractionEnded'], arrowY), xytext=(df_event.loc['InteractionEnded'], textY), ha='right', arrowprops=arrowprops, bbox=bbox_props)` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 149 | `report_all_results.py:233:8` | `ax.text(textX, textY, 'Area=%.2fJ' % energy_consumption, ha='center', va='center', size=15, bbox=bbox_props)` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 150 | `report_all_results.py:234:4` | `ax.set_ylabel('Power (W)')` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 151 | `report_all_results.py:235:4` | `ax.set_xlabel('Timestamp (s)')` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 152 | `report_all_results.py:236:4` | `ax.get_xaxis()` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 153 | `report_all_results.py:236:4` | `ax.get_xaxis().get_offset_text()` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 154 | `report_all_results.py:236:4` | `ax.get_xaxis().get_offset_text().set_visible(False)` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 155 | `report_all_results.py:239:8` | `ax.set_ylim([0, ymax])` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 156 | `report_all_results.py:245:4` | `ax.fill_between(df_energy_to_fill.index, 0, df_energy_to_fill[power_feature], edgecolor='none', facecolor='lightgray')` | `library / matplotlib` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-pandas-plot-axis-return: ax is returned by pandas Series.plot; identifying that external method result as a matplotlib Axis requires a pandas-matplotlib runtime contract not declared by project source |
| 157 | `tests/test_helper.py:11:4` | `view.touch()` | `library / com` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-android-helper-parameters: the shared helper parameters receive a non-convergent set of imported ViewClient results, unresolved local values, and missing-argument dead paths; project source does not prove one runtime receiver owner for every helper body call |
| 158 | `tests/test_helper.py:13:16` | `view.getXY()` | `library / com` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-android-helper-parameters: the shared helper parameters receive a non-convergent set of imported ViewClient results, unresolved local values, and missing-argument dead paths; project source does not prove one runtime receiver owner for every helper body call |
| 159 | `tests/test_helper.py:14:25` | `view.getWidth()` | `library / com` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-android-helper-parameters: the shared helper parameters receive a non-convergent set of imported ViewClient results, unresolved local values, and missing-argument dead paths; project source does not prove one runtime receiver owner for every helper body call |
| 160 | `tests/test_helper.py:14:46` | `view.getHeight()` | `library / com` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-android-helper-parameters: the shared helper parameters receive a non-convergent set of imported ViewClient results, unresolved local values, and missing-argument dead paths; project source does not prove one runtime receiver owner for every helper body call |
| 161 | `tests/test_helper.py:23:4` | `device.shell('input keyevent KEYCODE_BACK')` | `library / com` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-android-helper-parameters: the shared helper parameters receive a non-convergent set of imported ViewClient results, unresolved local values, and missing-argument dead paths; project source does not prove one runtime receiver owner for every helper body call |
| 162 | `tests/test_helper.py:45:4` | `device.drag(s, e, 500, 20, -1)` | `library / com` | `static_context` | `receiver_owner_requires_value_type_evidence` | review greenbenchmark-android-helper-parameters: the shared helper parameters receive a non-convergent set of imported ViewClient results, unresolved local values, and missing-argument dead paths; project source does not prove one runtime receiver owner for every helper body call |

### hfhd (22)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 163 | `hfhd/hd.py:766:19` | `x.between_time(stp[j], stp[j + 1], True, True)` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 164 | `hfhd/hd.py:768:22` | `x.between_time(stp[j + 1], stp[j], False, False)` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 165 | `hfhd/hf.py:175:30` | `x.dropna()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 166 | `hfhd/hf.py:176:20` | `x.dropna()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 167 | `hfhd/hf.py:176:20` | `x.dropna().to_numpy(dtype='float64')` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 168 | `hfhd/hf.py:299:15` | `data.to_numpy()` | `library / pandas` | `dynamic_probe` | `unbound_parameter_without_project_reference` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 169 | `hfhd/hf.py:301:15` | `data.to_numpy()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 170 | `hfhd/hf.py:407:23` | `x.dropna()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 171 | `hfhd/hf.py:408:21` | `x.dropna()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 172 | `hfhd/hf.py:408:21` | `x.dropna().to_numpy()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 173 | `hfhd/hf.py:433:8` | `np.log(price.dropna()).diff()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 174 | `hfhd/hf.py:433:15` | `price.dropna()` | `library / pandas` | `static_context` | `unbound_parameter_without_project_reference` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 175 | `hfhd/hf.py:434:16` | `y.mean()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 176 | `hfhd/hf.py:437:25` | `y_hat.mean()` | `library / pandas` | `static_context` | `bounded_or_pending` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 177 | `hfhd/hf.py:439:11` | `resid.cumsum()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 178 | `hfhd/hf.py:853:11` | `(sk - nk / nj * sj).to_numpy()` | `library / pandas` | `dynamic_probe` | `bounded_or_pending` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 179 | `hfhd/hf.py:1054:27` | `data.to_numpy()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 180 | `hfhd/hf.py:1519:27` | `data.to_numpy()` | `library / pandas` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review hfhd-pandas-parameter-and-external-results: these receivers originate from uncalled public parameters, pandas indexing or grouping results, or arithmetic over those external values; docstrings and probes describe intended runtime behavior but project source does not force one concrete pandas receiver type |
| 181 | `hfhd/hf.py:1973:31` | `a_values.reshape(-1, 1)` | `library / numpy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review hfhd-external-stacked-value-items: hayashi_yoshida assigns values = np.column_stack((np.zeros(p), values)) and forwards it to _hayashi_yoshida_pairwise. That function selects multidimensional rows/slices and passes them as a_values and b_values to _hayashi_yoshida. The project and current contracts do not establish column_stack's concrete return/item type. Docstrings specify intended ndarray inputs but do not bind runtime values; forwarding and slicing alone cannot prove both reshape receivers. |
| 182 | `hfhd/hf.py:1978:31` | `b_values.reshape(-1, 1)` | `library / numpy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review hfhd-external-stacked-value-items: hayashi_yoshida assigns values = np.column_stack((np.zeros(p), values)) and forwards it to _hayashi_yoshida_pairwise. That function selects multidimensional rows/slices and passes them as a_values and b_values to _hayashi_yoshida. The project and current contracts do not establish column_stack's concrete return/item type. Docstrings specify intended ndarray inputs but do not bind runtime values; forwarding and slicing alone cannot prove both reshape receivers. |
| 183 | `hfhd/sim.py:294:34` | `self.log_rets.cumsum(axis=0)` | `library / numpy` | `static_context` | `instance_attribute_owner_requires_value_type_evidence` | review hfhd-simulation-external-array-results: self.log_rets combines arrays with unbound constructor-supplied factor_loadings, industry_loadings and feature_beta. In simulate(), log_rets is multiplied by np.linalg.cholesky(corr_matrix).T. The project defines neither those input value types nor the external factorization result attribute and operator semantics. |
| 184 | `hfhd/sim.py:403:25` | `(log_rets - var / 2).cumsum(axis=0)` | `library / numpy` | `static_context` | `bounded_or_pending` | review hfhd-simulation-external-array-results: self.log_rets combines arrays with unbound constructor-supplied factor_loadings, industry_loadings and feature_beta. In simulate(), log_rets is multiplied by np.linalg.cholesky(corr_matrix).T. The project defines neither those input value types nor the external factorization result attribute and operator semantics. |

### polire (20)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 185 | `polire/base/base.py:118:15` | `pred_y.reshape(self.resolution, self.resolution)` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-polymorphic-grid-return-boundary: Base.predict_grid calls self._predict_grid across concrete local subclasses. One reachable subclass is CustomInterpolator, instantiated in usage3.py with external estimators; its implementation forwards self.reg.predict(...) unchanged. This is the same external-return boundary already reviewed at usage3.py:32. A known NumPy result in another subclass cannot prove the shared pred_y receiver across all callers. |
| 186 | `polire/idw/idw.py:83:17` | `(weights * self.y[:, None]).sum(axis=0)` | `library / numpy` | `static_context` | `instance_attribute_owner_requires_value_type_evidence` | review polire-fit-input-external-array-values: Base.fit forwards X/y unchanged to IDW._fit, NaturalNeighbor._fit and SpatialAverage._fit. usage.py constructs X/y using np.array (including DataFrame selections), and tests/polire_basic.py uses np.random.rand; neither producer has a concrete result-type contract in this analyzer. The methods store those values as self.X/self.y; is_row_in_array receives self.X. IDW weights/mask additionally use uncontracted np.power/np.equal results. SpatialAverage's mask is independently proven NumPy and mask.sum was fixed, but multiplying it by unproven self.y cannot establish the operator result owner. Local forwarding and .shape assertions do not supply the missing external type. |
| 187 | `polire/idw/idw.py:89:28` | `(self.y * mask).sum()` | `library / numpy` | `static_context` | `instance_attribute_owner_requires_value_type_evidence` | review polire-fit-input-external-array-values: Base.fit forwards X/y unchanged to IDW._fit, NaturalNeighbor._fit and SpatialAverage._fit. usage.py constructs X/y using np.array (including DataFrame selections), and tests/polire_basic.py uses np.random.rand; neither producer has a concrete result-type contract in this analyzer. The methods store those values as self.X/self.y; is_row_in_array receives self.X. IDW weights/mask additionally use uncontracted np.power/np.equal results. SpatialAverage's mask is independently proven NumPy and mask.sum was fixed, but multiplying it by unproven self.y cannot establish the operator result owner. Local forwarding and .shape assertions do not supply the missing external type. |
| 188 | `polire/natural_neighbors/natural_neighbors.py:16:24` | `arr.tolist()` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-fit-input-external-array-values: Base.fit forwards X/y unchanged to IDW._fit, NaturalNeighbor._fit and SpatialAverage._fit. usage.py constructs X/y using np.array (including DataFrame selections), and tests/polire_basic.py uses np.random.rand; neither producer has a concrete result-type contract in this analyzer. The methods store those values as self.X/self.y; is_row_in_array receives self.X. IDW weights/mask additionally use uncontracted np.power/np.equal results. SpatialAverage's mask is independently proven NumPy and mask.sum was fixed, but multiplying it by unproven self.y cannot establish the operator result owner. Local forwarding and .shape assertions do not supply the missing external type. |
| 189 | `polire/nsgp/nsgp.py:65:42` | `((S - self._X) ** 2).sum(axis=1)` | `library / numpy` | `static_context` | `instance_attribute_owner_requires_value_type_evidence` | review polire-uncalled-nsgp-input-and-kernel-results: The only NSGP construction in this fixture is in tests/polire_basic.py:test_nsgp, which is marked pytest.skip and whose NSGP import is commented out. No valid project call binds NSGP training/prediction inputs. _X/S and their items therefore lack concrete values. The _Kernel return additionally sums accumulators initialized by external np.zeros calls and populated using external learned-kernel results; those result types are not supplied by project code or current contracts. Neither the class name nor ndarray-shaped arithmetic proves these receiver owners. |
| 190 | `polire/nsgp/nsgp.py:150:42` | `s_vec.sum()` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-uncalled-nsgp-input-and-kernel-results: The only NSGP construction in this fixture is in tests/polire_basic.py:test_nsgp, which is marked pytest.skip and whose NSGP import is commented out. No valid project call binds NSGP training/prediction inputs. _X/S and their items therefore lack concrete values. The _Kernel return additionally sums accumulators initialized by external np.zeros calls and populated using external learned-kernel results; those result types are not supplied by project code or current contracts. Neither the class name nor ndarray-shaped arithmetic proves these receiver owners. |
| 191 | `polire/nsgp/nsgp.py:154:46` | `s_vec.sum()` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-uncalled-nsgp-input-and-kernel-results: The only NSGP construction in this fixture is in tests/polire_basic.py:test_nsgp, which is marked pytest.skip and whose NSGP import is commented out. No valid project call binds NSGP training/prediction inputs. _X/S and their items therefore lack concrete values. The _Kernel return additionally sums accumulators initialized by external np.zeros calls and populated using external learned-kernel results; those result types are not supplied by project code or current contracts. Neither the class name nor ndarray-shaped arithmetic proves these receiver owners. |
| 192 | `polire/nsgp/nsgp.py:235:12` | `KX_test.dot(self._KX_inv)` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-uncalled-nsgp-input-and-kernel-results: The only NSGP construction in this fixture is in tests/polire_basic.py:test_nsgp, which is marked pytest.skip and whose NSGP import is commented out. No valid project call binds NSGP training/prediction inputs. _X/S and their items therefore lack concrete values. The _Kernel return additionally sums accumulators initialized by external np.zeros calls and populated using external learned-kernel results; those result types are not supplied by project code or current contracts. Neither the class name nor ndarray-shaped arithmetic proves these receiver owners. |
| 193 | `polire/nsgp/nsgp.py:235:12` | `KX_test.dot(self._KX_inv).dot(self._y - self._y.mean())` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-uncalled-nsgp-input-and-kernel-results: The only NSGP construction in this fixture is in tests/polire_basic.py:test_nsgp, which is marked pytest.skip and whose NSGP import is commented out. No valid project call binds NSGP training/prediction inputs. _X/S and their items therefore lack concrete values. The _Kernel return additionally sums accumulators initialized by external np.zeros calls and populated using external learned-kernel results; those result types are not supplied by project code or current contracts. Neither the class name nor ndarray-shaped arithmetic proves these receiver owners. |
| 194 | `polire/nsgp/nsgp.py:239:44` | `KX_test.dot(self._KX_inv)` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-uncalled-nsgp-input-and-kernel-results: The only NSGP construction in this fixture is in tests/polire_basic.py:test_nsgp, which is marked pytest.skip and whose NSGP import is commented out. No valid project call binds NSGP training/prediction inputs. _X/S and their items therefore lack concrete values. The _Kernel return additionally sums accumulators initialized by external np.zeros calls and populated using external learned-kernel results; those result types are not supplied by project code or current contracts. Neither the class name nor ndarray-shaped arithmetic proves these receiver owners. |
| 195 | `polire/nsgp/nsgp.py:239:44` | `KX_test.dot(self._KX_inv).dot(KX_test.T)` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-uncalled-nsgp-input-and-kernel-results: The only NSGP construction in this fixture is in tests/polire_basic.py:test_nsgp, which is marked pytest.skip and whose NSGP import is commented out. No valid project call binds NSGP training/prediction inputs. _X/S and their items therefore lack concrete values. The _Kernel return additionally sums accumulators initialized by external np.zeros calls and populated using external learned-kernel results; those result types are not supplied by project code or current contracts. Neither the class name nor ndarray-shaped arithmetic proves these receiver owners. |
| 196 | `polire/preprocessing/sptial_features.py:103:19` | `self_y_local[:, None].repeat(lonlat.shape[0], 1)` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-uncalled-spatial-features-training-value: SpatialFeatures is defined and re-exported, but no project code constructs or invokes it. self.y is assigned only from fit's y parameter, and transform selects self.y[trn_mask] before repeat(). The np.ndarray annotation states the intended input contract but provides no concrete caller value or assignment. No library owner is inferred from an unbound annotated parameter. |
| 197 | `polire/spatial/spatial.py:64:15` | `(self.y * mask).sum(axis=1)` | `library / numpy` | `static_context` | `instance_attribute_owner_requires_value_type_evidence` | review polire-fit-input-external-array-values: Base.fit forwards X/y unchanged to IDW._fit, NaturalNeighbor._fit and SpatialAverage._fit. usage.py constructs X/y using np.array (including DataFrame selections), and tests/polire_basic.py uses np.random.rand; neither producer has a concrete result-type contract in this analyzer. The methods store those values as self.X/self.y; is_row_in_array receives self.X. IDW weights/mask additionally use uncontracted np.power/np.equal results. SpatialAverage's mask is independently proven NumPy and mask.sum was fixed, but multiplying it by unproven self.y cannot establish the operator result owner. Local forwarding and .shape assertions do not supply the missing external type. |
| 198 | `polire/utils/gridding.py:33:12` | `y.min()` | `library / numpy` | `static_context` | `unbound_parameter_without_project_reference` | review polire-unreferenced-gridding-parameters: make_grid has no project-source reference, and find_closest's only apparent call is commented out. Their x, y, and grid parameters have no defaults or type annotations. Array-like descriptions in docstrings and NumPy-style method names express intended usage but do not establish concrete receiver owners from executable project source. |
| 199 | `polire/utils/gridding.py:34:12` | `y.max()` | `library / numpy` | `static_context` | `unbound_parameter_without_project_reference` | review polire-unreferenced-gridding-parameters: make_grid has no project-source reference, and find_closest's only apparent call is commented out. Their x, y, and grid parameters have no defaults or type annotations. Array-like descriptions in docstrings and NumPy-style method names express intended usage but do not establish concrete receiver owners from executable project source. |
| 200 | `polire/utils/gridding.py:35:12` | `x.min()` | `library / numpy` | `static_context` | `unbound_parameter_without_project_reference` | review polire-unreferenced-gridding-parameters: make_grid has no project-source reference, and find_closest's only apparent call is commented out. Their x, y, and grid parameters have no defaults or type annotations. Array-like descriptions in docstrings and NumPy-style method names express intended usage but do not establish concrete receiver owners from executable project source. |
| 201 | `polire/utils/gridding.py:36:12` | `x.max()` | `library / numpy` | `static_context` | `unbound_parameter_without_project_reference` | review polire-unreferenced-gridding-parameters: make_grid has no project-source reference, and find_closest's only apparent call is commented out. Their x, y, and grid parameters have no defaults or type annotations. Array-like descriptions in docstrings and NumPy-style method names express intended usage but do not establish concrete receiver owners from executable project source. |
| 202 | `polire/utils/gridding.py:67:9` | `grid[0].ravel()` | `library / numpy` | `static_context` | `unbound_parameter_without_project_reference` | review polire-unreferenced-gridding-parameters: make_grid has no project-source reference, and find_closest's only apparent call is commented out. Their x, y, and grid parameters have no defaults or type annotations. Array-like descriptions in docstrings and NumPy-style method names express intended usage but do not establish concrete receiver owners from executable project source. |
| 203 | `polire/utils/gridding.py:67:26` | `grid[1].ravel()` | `library / numpy` | `static_context` | `unbound_parameter_without_project_reference` | review polire-unreferenced-gridding-parameters: make_grid has no project-source reference, and find_closest's only apparent call is commented out. Their x, y, and grid parameters have no defaults or type annotations. Array-like descriptions in docstrings and NumPy-style method names express intended usage but do not establish concrete receiver owners from executable project source. |
| 204 | `usage3.py:32:8` | `r.predict_grid((0, 3), (0, 3)).reshape(100, 100)` | `library / numpy` | `static_context` | `receiver_owner_requires_value_type_evidence` | review polire-custom-estimator-prediction-result: r is a local CustomInterpolator wrapping one of five external estimator constructors. predict_grid reaches CustomInterpolator._predict_grid and self.reg.predict(...). The wrapper forwards the external prediction unchanged; project source cannot prove the return type shared by those external model implementations. |

### political-polarisation (21)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 205 | `analyze_hashtag.py:25:20` | `df['hashtag'].astype('string')` | `library / pandas` | `static_context` | `unbound_parameter_without_project_reference` | review political-pandas-accessor-results: trim_hashtag has no project call site; df_name is derived from a parameter through pandas subscript, str accessor and stack/reset_index results. The string dtype argument supplies intended runtime context but no project-defined return or accessor implementation. |
| 206 | `analyze_hashtag.py:27:14` | `df_name.str.split(',', expand=True)` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-pandas-accessor-results: trim_hashtag has no project call site; df_name is derived from a parameter through pandas subscript, str accessor and stack/reset_index results. The string dtype argument supplies intended runtime context but no project-defined return or accessor implementation. |
| 207 | `analyze_hashtag.py:28:14` | `df_name.stack()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-pandas-accessor-results: trim_hashtag has no project call site; df_name is derived from a parameter through pandas subscript, str accessor and stack/reset_index results. The string dtype argument supplies intended runtime context but no project-defined return or accessor implementation. |
| 208 | `analyze_hashtag.py:30:14` | `df_name.reset_index(level=1, drop=True)` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-pandas-accessor-results: trim_hashtag has no project call site; df_name is derived from a parameter through pandas subscript, str accessor and stack/reset_index results. The string dtype argument supplies intended runtime context but no project-defined return or accessor implementation. |
| 209 | `analyze_hashtag.py:30:14` | `df_name.reset_index(level=1, drop=True).to_numpy()` | `library / pandas` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-pandas-accessor-results: trim_hashtag has no project call site; df_name is derived from a parameter through pandas subscript, str accessor and stack/reset_index results. The string dtype argument supplies intended runtime context but no project-defined return or accessor implementation. |
| 210 | `analyze_hashtag.py:33:15` | `item.replace("'", '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 211 | `analyze_hashtag.py:33:15` | `item.replace("'", '').replace('{', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 212 | `analyze_hashtag.py:33:15` | `item.replace("'", '').replace('{', '').replace('}', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 213 | `analyze_hashtag.py:33:15` | `item.replace("'", '').replace('{', '').replace('}', '').replace('set()', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 214 | `analyze_hashtag.py:33:15` | `item.replace("'", '').replace('{', '').replace('}', '').replace('set()', '').replace(' ', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 215 | `analyze_hashtag.py:33:15` | `item.replace("'", '').replace('{', '').replace('}', '').replace('set()', '').replace(' ', '').replace('#', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 216 | `analyze_hashtag.py:53:11` | `x.replace("'", '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 217 | `analyze_hashtag.py:53:11` | `x.replace("'", '').replace('{', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 218 | `analyze_hashtag.py:53:11` | `x.replace("'", '').replace('{', '').replace('}', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 219 | `analyze_hashtag.py:53:11` | `x.replace("'", '').replace('{', '').replace('}', '').replace('set()', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 220 | `analyze_hashtag.py:53:11` | `x.replace("'", '').replace('{', '').replace('}', '').replace('set()', '').replace(' ', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 221 | `analyze_hashtag.py:53:11` | `x.replace("'", '').replace('{', '').replace('}', '').replace('set()', '').replace(' ', '').replace('#', '')` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 222 | `analyze_hashtag.py:53:11` | `x.replace("'", '').replace('{', '').replace('}', '').replace('set()', '').replace(' ', '').replace('#', '').strip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 223 | `analyze_hashtag.py:61:23` | `row['mentions'].strip()` | `python / python` | `manual_reasoned` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 224 | `analyze_hashtag.py:61:23` | `row['mentions'].strip().split(',')` | `python / python` | `manual_reasoned` | `receiver_owner_requires_value_type_evidence` | review political-external-text-elements: item is iterated from to_numpy output, clean_list receives values from external pandas map callbacks, and row['mentions'] is selected from external iterrows output. Python string labels describe the intended text pipeline; neither external iteration nor callback element type is specified by executable project source. |
| 225 | `analyze_hashtag.py:63:34` | `mention.strip()` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review political-split-element: mention is iterated from split on a value selected from pandas iterrows. The project does not independently prove that row['mentions'] is str; assuming a builtin split result from the method name alone would guess the receiver type. |

### scrapping (8)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 226 | `jojo.py:53:23` | `link.find(':')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review scrapping-beautifulsoup-attribute-value: link is returned by BeautifulSoup Tag.get from remote HTML; project source does not define or constrain the external attribute value type |
| 227 | `jojo.py:64:31` | `link.find(':')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review scrapping-beautifulsoup-attribute-value: link is returned by BeautifulSoup Tag.get from remote HTML; project source does not define or constrain the external attribute value type |
| 228 | `jojo.py:82:23` | `link.find(':')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review scrapping-beautifulsoup-attribute-value: link is returned by BeautifulSoup Tag.get from remote HTML; project source does not define or constrain the external attribute value type |
| 229 | `jojo.py:93:31` | `link.find(':')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review scrapping-beautifulsoup-attribute-value: link is returned by BeautifulSoup Tag.get from remote HTML; project source does not define or constrain the external attribute value type |
| 230 | `jojo1.py:53:23` | `link.find(':')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review scrapping-beautifulsoup-attribute-value: link is returned by BeautifulSoup Tag.get from remote HTML; project source does not define or constrain the external attribute value type |
| 231 | `jojo1.py:64:31` | `link.find(':')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review scrapping-beautifulsoup-attribute-value: link is returned by BeautifulSoup Tag.get from remote HTML; project source does not define or constrain the external attribute value type |
| 232 | `jojo1.py:82:23` | `link.find(':')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review scrapping-beautifulsoup-attribute-value: link is returned by BeautifulSoup Tag.get from remote HTML; project source does not define or constrain the external attribute value type |
| 233 | `jojo1.py:93:31` | `link.find(':')` | `python / python` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review scrapping-beautifulsoup-attribute-value: link is returned by BeautifulSoup Tag.get from remote HTML; project source does not define or constrain the external attribute value type |

### simulation (6)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 234 | `analytical.py:44:8` | `g.bounding_box()` | `library / porepy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review simulation-porepy-grid-object: g is selected from the result of PorePy's external mesh and GridBucket APIs; project source contains no Grid return-type contract |
| 235 | `analytical.py:45:8` | `g.bounding_box()` | `library / porepy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review simulation-porepy-grid-object: g is selected from the result of PorePy's external mesh and GridBucket APIs; project source contains no Grid return-type contract |
| 236 | `analytical.py:51:14` | `g.tags['domain_boundary_faces'].nonzero()` | `library / numpy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review simulation-porepy-tag-value: the domain_boundary_faces item is read from an external PorePy Grid tags mapping; its NumPy value type is not defined by project source |
| 237 | `data.py:153:8` | `g.bounding_box()` | `library / porepy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review simulation-porepy-grid-object: g is selected from the result of PorePy's external mesh and GridBucket APIs; project source contains no Grid return-type contract |
| 238 | `data.py:154:8` | `g.bounding_box()` | `library / porepy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review simulation-porepy-grid-object: g is selected from the result of PorePy's external mesh and GridBucket APIs; project source contains no Grid return-type contract |
| 239 | `data.py:157:14` | `g.tags['domain_boundary_faces'].nonzero()` | `library / numpy` | `dynamic_probe` | `receiver_owner_requires_value_type_evidence` | review simulation-porepy-tag-value: the domain_boundary_faces item is read from an external PorePy Grid tags mapping; its NumPy value type is not defined by project source |

### tensorflow1 (1)

| # | Location | Expression | GT owner | Verification | Source boundary | Why `unknown` |
|---:|---|---|---|---|---|---|
| 240 | `tf_decorator.py:19:8` | `config.update({'units': self.units})` | `python / python` | `static_context` | `receiver_owner_requires_value_type_evidence` | review tensorflow-external-base-config-result: config is returned by TensorFlow Layer.get_config through super(); the external base implementation and its concrete mapping result type are outside project source |

## Release Rule

1. Every `fix_1_0_5` entry must either become a primary hit or be reclassified with reviewed evidence.
2. `accepted_unknown` entries remain scored as GT misses, but do not require a guessed exact owner for release.
3. `ground_truth_correction` entries must update the canonical GT before algorithm work continues.
4. No mismatch may remain without a disposition.
5. The stable 1.0.5 release requires zero `fix_1_0_5` and zero `ground_truth_correction` entries.
