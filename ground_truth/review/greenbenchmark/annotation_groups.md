# greenbenchmark -- Annotation Groups (64 groups, 229 records)

## Summary

| Evidence | Groups | Records | Needs Human |
|----------|--------|---------|-------------|
| static_obvious | 3 | 5 | 0 |
| static_context | 18 | 27 | 0 |
| manual_reasoned | 43 | 197 | 197 |
| **Total** | **64** | **229** | **197** |

## Group 1: vc -> ?/? (57 records)

| Evidence | manual_reasoned |
| Needs human | yes (57/57) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>ViewClient(device, serialno, **kwargs2)</code> @ tests/writely-pro-view_holder.py:16 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:23
- <code>vc.findViewById('me.writeily:id/fab_expand_menu_button')</code> -- tests/writely-pro-view_holder.py:24
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:26
- <code>vc.findViewById('me.writeily:id/create_folder')</code> -- tests/writely-pro-view_holder.py:27
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:28
- <code>vc.findViewWithText(u'Create')</code> -- tests/writely-pro-view_holder.py:30
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:37
- <code>vc.findViewById('me.writeily:id/fab_expand_menu_button')</code> -- tests/writely-pro-view_holder.py:38
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:40
- <code>vc.findViewById('me.writeily:id/create_note')</code> -- tests/writely-pro-view_holder.py:41
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:43
- <code>vc.findViewById('me.writeily:id/note_content')</code> -- tests/writely-pro-view_holder.py:45
- <code>vc.findViewById('id/no_id/14')</code> -- tests/writely-pro-view_holder.py:45
- <code>vc.findViewWithContentDescription(u'Navigate up')</code> -- tests/writely-pro-view_holder.py:47
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:49
- <code>vc.findViewWithContentDescription(u'More options')</code> -- tests/writely-pro-view_holder.py:50
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:51
- <code>vc.findViewWithText(u'Settings')</code> -- tests/writely-pro-view_holder.py:52
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:53
- <code>vc.findViewWithContentDescription(u'Navigate up')</code> -- tests/writely-pro-view_holder.py:54
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:64
- <code>vc.findViewWithText('folder-one')</code> -- tests/writely-pro-view_holder.py:65
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:75
- <code>vc.findViewWithText('folder-two')</code> -- tests/writely-pro-view_holder.py:76
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:79
- <code>vc.findViewWithText('folder-two')</code> -- tests/writely-pro-view_holder.py:80
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:83
- <code>vc.findViewWithText('folder-two')</code> -- tests/writely-pro-view_holder.py:84
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:89
- <code>vc.findViewWithText('folder-one')</code> -- tests/writely-pro-view_holder.py:90
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:96
- <code>vc.findViewWithText('folder-two')</code> -- tests/writely-pro-view_holder.py:97
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:99
- <code>vc.findViewWithText(u'note-two')</code> -- tests/writely-pro-view_holder.py:101
- <code>vc.findViewWithText(u'note-three')</code> -- tests/writely-pro-view_holder.py:102
- <code>vc.findViewWithText(u'note-four')</code> -- tests/writely-pro-view_holder.py:103
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:105
- <code>vc.findViewWithContentDescription(u'Move')</code> -- tests/writely-pro-view_holder.py:106
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:107
- <code>vc.findViewWithText(u'folder-one')</code> -- tests/writely-pro-view_holder.py:108
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:109
- <code>vc.findViewWithText(u'Move here')</code> -- tests/writely-pro-view_holder.py:110
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:111
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:116
- <code>vc.findViewWithText(u'folder-one')</code> -- tests/writely-pro-view_holder.py:118
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:120
- <code>vc.findViewWithContentDescription(u'Move')</code> -- tests/writely-pro-view_holder.py:121
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:122
- <code>vc.findViewWithText(u'folder-three')</code> -- tests/writely-pro-view_holder.py:123
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:124
- <code>vc.findViewWithText(u'Move here')</code> -- tests/writely-pro-view_holder.py:125
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:127
- <code>vc.findViewWithText(u'folder-three')</code> -- tests/writely-pro-view_holder.py:128
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:130
- <code>vc.findViewWithContentDescription(u'Delete')</code> -- tests/writely-pro-view_holder.py:131
- <code>vc.dump(window='-1')</code> -- tests/writely-pro-view_holder.py:132
- <code>vc.findViewWithText(u'OK')</code> -- tests/writely-pro-view_holder.py:133

**All bindings (1 unique):**
- <code>tests/writely-pro-view_holder.py</code> L16: <code>ViewClient(device, serialno, **kwargs2)</code>

## Group 2: vc -> ?/? (23 records)

| Evidence | manual_reasoned |
| Needs human | yes (23/23) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>ViewClient(device, serialno, **kwargs2)</code> @ tests/uhabits.py:16 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:20
- <code>vc.findViewWithContentDescription(u'Add habit')</code> -- tests/uhabits.py:22
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:23
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:25
- <code>vc.findViewWithText(u'Save')</code> -- tests/uhabits.py:26
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:33
- <code>vc.findViewWithTextOrRaise(habit_name)</code> -- tests/uhabits.py:34
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:35
- <code>vc.findViewWithContentDescription(u'Navigate up')</code> -- tests/uhabits.py:38
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:41
- <code>vc.findViewById('org.isoron.uhabits:id/next')</code> -- tests/uhabits.py:42
- <code>vc.findViewById('id/no_id/23')</code> -- tests/uhabits.py:42
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:49
- <code>vc.findViewWithContentDescription(u'More options')</code> -- tests/uhabits.py:50
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:51
- <code>vc.findViewWithText(u'About')</code> -- tests/uhabits.py:52
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:66
- <code>vc.findViewWithText(habit_name)</code> -- tests/uhabits.py:69
- <code>vc.findViewWithContentDescription(u'More options')</code> -- tests/uhabits.py:70
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:71
- <code>vc.findViewWithText('Delete')</code> -- tests/uhabits.py:72
- <code>vc.dump(window='-1')</code> -- tests/uhabits.py:73
- <code>vc.findViewWithText('OK')</code> -- tests/uhabits.py:74

**All bindings (1 unique):**
- <code>tests/uhabits.py</code> L16: <code>ViewClient(device, serialno, **kwargs2)</code>

## Group 3: vc -> ?/? (16 records)

| Evidence | manual_reasoned |
| Needs human | yes (16/16) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>ViewClient(device, serialno, **kwargs2)</code> @ tests/talalarmo.py:16 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>vc.dump(window='-1')</code> -- tests/talalarmo.py:18
- <code>vc.findViewWithText(u'Tap here\nto set alarm')</code> -- tests/talalarmo.py:19
- <code>vc.dump(window='-1')</code> -- tests/talalarmo.py:23
- <code>vc.findViewById('id/no_id/19')</code> -- tests/talalarmo.py:24
- <code>vc.findViewById('id/no_id/9')</code> -- tests/talalarmo.py:24
- <code>vc.findViewWithText(u'OFF')</code> -- tests/talalarmo.py:25
- <code>vc.findViewWithText(u'ON')</code> -- tests/talalarmo.py:25
- <code>vc.findViewById('id/no_id/21')</code> -- tests/talalarmo.py:26
- <code>vc.findViewById('id/no_id/11')</code> -- tests/talalarmo.py:26
- <code>vc.dump(window='-1')</code> -- tests/talalarmo.py:32
- <code>vc.dump(window='-1')</code> -- tests/talalarmo.py:41
- <code>vc.findViewWithText(u'Settings')</code> -- tests/talalarmo.py:42
- <code>vc.dump(window='-1')</code> -- tests/talalarmo.py:44
- <code>vc.findViewWithText(u'Theme')</code> -- tests/talalarmo.py:45
- <code>vc.dump(window='-1')</code> -- tests/talalarmo.py:46
- <code>vc.findViewWithText(next_theme)</code> -- tests/talalarmo.py:47

**All bindings (1 unique):**
- <code>tests/talalarmo.py</code> L16: <code>ViewClient(device, serialno, **kwargs2)</code>

## Group 4: vc -> ?/? (14 records)

| Evidence | manual_reasoned |
| Needs human | yes (14/14) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>ViewClient(device, serialno, **kwargs2)</code> @ tests/gnucash.py:16 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>vc.dump(window='-1')</code> -- tests/gnucash.py:18
- <code>vc.findViewWithTextOrRaise(u'Next')</code> -- tests/gnucash.py:19
- <code>vc.dump(window='-1')</code> -- tests/gnucash.py:23
- <code>vc.findViewWithTextOrRaise(u'Disable crash reports')</code> -- tests/gnucash.py:24
- <code>vc.dump(window='-1')</code> -- tests/gnucash.py:27
- <code>vc.findViewWithText(u'Allow')</code> -- tests/gnucash.py:28
- <code>vc.dump(window='-1')</code> -- tests/gnucash.py:31
- <code>vc.findViewWithTextOrRaise(u'Assets')</code> -- tests/gnucash.py:32
- <code>vc.dump(window='-1')</code> -- tests/gnucash.py:33
- <code>vc.findViewWithContentDescriptionOrRaise(u'More options')</code> -- tests/gnucash.py:34
- <code>vc.dump(window='-1')</code> -- tests/gnucash.py:36
- <code>vc.findViewWithTextOrRaise(u'Edit Account')</code> -- tests/gnucash.py:37
- <code>vc.dump(window='-1')</code> -- tests/gnucash.py:43
- <code>vc.findViewWithTextOrRaise(account)</code> -- tests/gnucash.py:44

**All bindings (1 unique):**
- <code>tests/gnucash.py</code> L16: <code>ViewClient(device, serialno, **kwargs2)</code>

## Group 5: ax -> ?/? (9 records)

| Evidence | manual_reasoned |
| Needs human | yes (9/9) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>df_energy[power_feature].plot(color='darkblue',linewidth=1.0)</code> @ report_all_results.py:214 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>ax.axvline(timestamp, color='darkblue', linestyle='--', alpha=0.5, linewidth=2)</code> -- report_all_results.py:217
- <code>ax.annotate('t0 - InteractionStarted', xy=(df_event.loc['InteractionStarted'], arrowY), xytext=(df_e</code> -- report_all_results.py:223
- <code>ax.annotate('tn - InteractionEnded', xy=(df_event.loc['InteractionEnded'], arrowY), xytext=(df_event</code> -- report_all_results.py:226
- <code>ax.text(textX, textY, 'Area=%.2fJ' % energy_consumption, ha='center', va='center', size=15, bbox=bbo</code> -- report_all_results.py:233
- <code>ax.set_ylabel('Power (W)')</code> -- report_all_results.py:234
- <code>ax.set_xlabel('Timestamp (s)')</code> -- report_all_results.py:235
- <code>ax.get_xaxis()</code> -- report_all_results.py:236
- <code>ax.set_ylim([0, ymax])</code> -- report_all_results.py:239
- <code>ax.fill_between(df_energy_to_fill.index, 0, df_energy_to_fill[power_feature], edgecolor='none', face</code> -- report_all_results.py:245

**All bindings (1 unique):**
- <code>report_all_results.py</code> L214: <code>df_energy[power_feature].plot(color='darkblue',linewidth=1.0)</code>

## Group 6: vc -> ?/? (8 records)

| Evidence | manual_reasoned |
| Needs human | yes (8/8) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>ViewClient(device, serialno, **kwargs2)</code> @ tests/simplegallery.py:16 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>vc.dump(window='-1')</code> -- tests/simplegallery.py:18
- <code>vc.findViewWithText(u'Allow')</code> -- tests/simplegallery.py:19
- <code>vc.dump(window='-1')</code> -- tests/simplegallery.py:26
- <code>vc.findViewById('com.simplemobiletools.gallery:id/dir_name')</code> -- tests/simplegallery.py:27
- <code>vc.findViewById('id/no_id/18')</code> -- tests/simplegallery.py:27
- <code>vc.dump(window='-1')</code> -- tests/simplegallery.py:30
- <code>vc.findViewById('com.simplemobiletools.gallery:id/media_item_holder')</code> -- tests/simplegallery.py:31
- <code>vc.findViewById('id/no_id/11')</code> -- tests/simplegallery.py:31

**All bindings (1 unique):**
- <code>tests/simplegallery.py</code> L16: <code>ViewClient(device, serialno, **kwargs2)</code>

## Group 7: df_energy[power_feature] -> ?/? (6 records)

| Evidence | manual_reasoned |
| Needs human | yes (6/6) |
| Reason | LOCAL_DEFINITION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_energy[power_feature].plot(color='darkblue', linewidth=1.0)</code> -- report_all_results.py:214
- <code>df_energy[power_feature].max()</code> -- report_all_results.py:219
- <code>df_energy[power_feature].min()</code> -- report_all_results.py:219
- <code>df_energy[power_feature].max()</code> -- report_all_results.py:220
- <code>df_energy[power_feature].max()</code> -- report_all_results.py:232
- <code>df_energy[power_feature].min()</code> -- report_all_results.py:232


## Group 8: vc -> ?/? (6 records)

| Evidence | manual_reasoned |
| Needs human | yes (6/6) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>ViewClient(device, serialno, **kwargs2)</code> @ tests/acrylicpaint.py:16 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>vc.dump(window='-1')</code> -- tests/acrylicpaint.py:18
- <code>vc.findViewWithTextOrRaise(u'Continue')</code> -- tests/acrylicpaint.py:19
- <code>vc.dump(window='-1')</code> -- tests/acrylicpaint.py:20
- <code>vc.findViewWithContentDescription(u'Color')</code> -- tests/acrylicpaint.py:21
- <code>vc.dump(window='-1')</code> -- tests/acrylicpaint.py:28
- <code>vc.findViewById('id/no_id/1')</code> -- tests/acrylicpaint.py:29

**All bindings (1 unique):**
- <code>tests/acrylicpaint.py</code> L16: <code>ViewClient(device, serialno, **kwargs2)</code>

## Group 9: action -> ?/? (5 records)

| Evidence | manual_reasoned |
| Needs human | yes (5/5) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>for target</code> @ run_cached_script.py:45 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>action.get('type')</code> -- run_cached_script.py:46
- <code>action.get('position')</code> -- run_cached_script.py:48
- <code>action.get('text')</code> -- run_cached_script.py:51
- <code>action.get('duration')</code> -- run_cached_script.py:53
- <code>action.get('position')</code> -- run_cached_script.py:59

**All bindings (1 unique):**
- <code>run_cached_script.py</code> L45: <code>for target</code>

## Group 10: v -> ?/? (5 records)

| Evidence | manual_reasoned |
| Needs human | yes (5/5) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>v.armW.mean()</code> -- report_all_results.py:32
- <code>v.memW.mean()</code> -- report_all_results.py:33
- <code>v.g3dW.mean()</code> -- report_all_results.py:34
- <code>v.kfcW.mean()</code> -- report_all_results.py:35
- <code>v.temperature.mean()</code> -- report_all_results.py:36


## Group 11: view -> ?/? (4 records)

| Evidence | manual_reasoned |
| Needs human | yes (4/4) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter view</code> @ tests/test_helper.py:10 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>view.touch()</code> -- tests/test_helper.py:11
- <code>view.getXY()</code> -- tests/test_helper.py:13
- <code>view.getWidth()</code> -- tests/test_helper.py:14
- <code>view.getHeight()</code> -- tests/test_helper.py:14

**All bindings (1 unique):**
- <code>tests/test_helper.py</code> L10: <code>parameter view</code>

## Group 12: parser -> library/argparse (4 records)

| Evidence | static_context |
| Needs human | no (0/4) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>argparse.ArgumentParser(description='Report a set of Odroid Energy Experiment Re</code> @ report_all_results.py:18 |
| Owner | argparse |
| Proposed GT | library / argparse |

**Representative expressions:**

- <code>parser.add_argument('--all_figures', action='store_true')</code> -- report_all_results.py:19
- <code>parser.add_argument('--single_experiment', action='store_true')</code> -- report_all_results.py:20
- <code>parser.add_argument('results_directory', help='the directory with all the results')</code> -- report_all_results.py:21
- <code>parser.parse_args()</code> -- report_all_results.py:23

**All bindings (1 unique):**
- <code>report_all_results.py</code> L18: <code>argparse.ArgumentParser(description='Report a set of Odroid Energy Experiment Re</code>

## Group 13: df -> library/pandas (4 records)

| Evidence | static_context |
| Needs human | no (0/4) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>pandas.DataFrame(experiments_report_data)</code> @ report_all_results.py:89 |
| Owner | pandas |
| Proposed GT | library / pandas |

**Representative expressions:**

- <code>df.to_csv(dir_path + '/_results.csv')</code> -- report_all_results.py:90
- <code>df.groupby('experiment')</code> -- report_all_results.py:99
- <code>df.dropna(inplace=True)</code> -- report_all_results.py:100
- <code>df.groupby('experiment')</code> -- report_all_results.py:105

**All bindings (1 unique):**
- <code>report_all_results.py</code> L89: <code>pandas.DataFrame(experiments_report_data)</code>

## Group 14: group[energy_feature] -> ?/? (3 records)

| Evidence | manual_reasoned |
| Needs human | yes (3/3) |
| Reason | LOCAL_DEFINITION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>group[energy_feature].mean()</code> -- report_all_results.py:184
- <code>group[energy_feature].std()</code> -- report_all_results.py:185
- <code>group[energy_feature].mean()</code> -- report_all_results.py:192


## Group 15: view -> ?/? (3 records)

| Evidence | manual_reasoned |
| Needs human | yes (3/3) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter view</code> @ tests/test_helper.py:32 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>view.getXY()</code> -- tests/test_helper.py:33
- <code>view.getWidth()</code> -- tests/test_helper.py:34
- <code>view.getHeight()</code> -- tests/test_helper.py:34

**All bindings (1 unique):**
- <code>tests/test_helper.py</code> L32: <code>parameter view</code>

## Group 16: device -> ?/? (2 records)

| Evidence | manual_reasoned |
| Needs human | yes (2/2) |
| Reason | TRANSITIVE_IMPORT |
| Proposed GT | ? / ? |

**All expressions:**

- <code>device.drag(s, e, 500, 20, -1)</code> -- run_cached_script.py:22
- <code>device.drag(s, e, 500, 20, -1)</code> -- run_cached_script.py:29


## Group 17: device -> ?/? (2 records)

| Evidence | manual_reasoned |
| Needs human | yes (2/2) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>(tuple) ViewClient.connectToDeviceOrExit(**kwargs1)</code> @ run_cached_script.py:37 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>device.type(action.get('text'))</code> -- run_cached_script.py:51
- <code>device.shell('input keyevent KEYCODE_BACK')</code> -- run_cached_script.py:62

**All bindings (1 unique):**
- <code>run_cached_script.py</code> L37: <code>(tuple) ViewClient.connectToDeviceOrExit(**kwargs1)</code>

## Group 18: df_experiment_pivot[energy_feature] -> ?/? (2 records)

| Evidence | manual_reasoned |
| Needs human | yes (2/2) |
| Reason | LOCAL_DEFINITION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_experiment_pivot[energy_feature].mean()</code> -- report_all_results.py:192
- <code>df_experiment_pivot[energy_feature].mean()</code> -- report_all_results.py:194


## Group 19: df_without_blankapp['experiment'] -> ?/? (2 records)

| Evidence | manual_reasoned |
| Needs human | yes (2/2) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_without_blankapp['experiment'].apply(lambda x: experiments_without_blankapp.index(x))</code> -- report_all_results.py:146
- <code>df_without_blankapp['experiment'].apply(lambda x: experiments_without_blankapp.index(x))</code> -- report_all_results.py:167


## Group 20: experiments_without_blankapp -> ?/? (2 records)

| Evidence | manual_reasoned |
| Needs human | yes (2/2) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>[experiment for experiment in experiments if experiment != 'blank-app']</code> @ report_all_results.py:106 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>experiments_without_blankapp.index(x)</code> -- report_all_results.py:146
- <code>experiments_without_blankapp.index(x)</code> -- report_all_results.py:167

**All bindings (1 unique):**
- <code>report_all_results.py</code> L106: <code>[experiment for experiment in experiments if experiment != 'blank-app']</code>

## Group 21: group -> ?/? (2 records)

| Evidence | manual_reasoned |
| Needs human | yes (2/2) |
| Reason | UNRESOLVED |
| Key binding | <code>parameter group</code> @ report_all_results.py:96 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>group.mean()</code> -- report_all_results.py:97
- <code>group.std()</code> -- report_all_results.py:97

**All bindings (1 unique):**
- <code>report_all_results.py</code> L96: <code>parameter group</code>

## Group 22: sample1 -> ?/? (2 records)

| Evidence | manual_reasoned |
| Needs human | yes (2/2) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter sample1</code> @ report_all_results.py:76 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>sample1.var()</code> -- report_all_results.py:79
- <code>sample1.mean()</code> -- report_all_results.py:81

**All bindings (1 unique):**
- <code>report_all_results.py</code> L76: <code>parameter sample1</code>

## Group 23: sample2 -> ?/? (2 records)

| Evidence | manual_reasoned |
| Needs human | yes (2/2) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter sample2</code> @ report_all_results.py:76 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>sample2.var()</code> -- report_all_results.py:80
- <code>sample2.mean()</code> -- report_all_results.py:81

**All bindings (1 unique):**
- <code>report_all_results.py</code> L76: <code>parameter sample2</code>

## Group 24: parser -> library/argparse (2 records)

| Evidence | static_context |
| Needs human | no (0/2) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>argparse.ArgumentParser(description='Run Android user interaction that is descri</code> @ run_cached_script.py:11 |
| Owner | argparse |
| Proposed GT | library / argparse |

**Representative expressions:**

- <code>parser.add_argument('json_file', help='the json file with interaction data')</code> -- run_cached_script.py:12
- <code>parser.parse_args()</code> -- run_cached_script.py:13

**All bindings (1 unique):**
- <code>run_cached_script.py</code> L11: <code>argparse.ArgumentParser(description='Run Android user interaction that is descri</code>

## Group 25: MonkeyRunner -> library/com (2 records)

| Evidence | static_obvious |
| Needs human | no (0/2) |
| Reason | TRANSITIVE_IMPORT |
| Key binding | <code>from com.android.monkeyrunner import MonkeyRunner</code> @ tests/hold.py:2 |
| Owner | com |
| Proposed GT | library / com |

**Representative expressions:**

- <code>MonkeyRunner.waitForConnection(timeout=60, deviceId=serialno)</code> -- tests/hold.py:7
- <code>MonkeyRunner.sleep(no_of_seconds)</code> -- tests/hold.py:11

**All bindings (1 unique):**
- <code>tests/hold.py</code> L2: <code>from com.android.monkeyrunner import MonkeyRunner</code>

## Group 26: ViewClient -> library/com (2 records)

| Evidence | static_obvious |
| Needs human | no (0/2) |
| Reason | TRANSITIVE_IMPORT |
| Key binding | <code>from com.dtmilano.android.viewclient import ViewClient</code> @ run_cached_script.py:9 |
| Owner | com |
| Proposed GT | library / com |

**Representative expressions:**

- <code>ViewClient.connectToDeviceOrExit(**kwargs1)</code> -- run_cached_script.py:37
- <code>ViewClient(device, serialno, **kwargs2)</code> -- run_cached_script.py:39

**All bindings (1 unique):**
- <code>run_cached_script.py</code> L9: <code>from com.dtmilano.android.viewclient import ViewClient</code>

## Group 27: device -> library/com (2 records)

| Evidence | static_context |
| Needs human | no (0/2) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>MonkeyRunner.waitForConnection(timeout = 60, deviceId = serialno)</code> @ tests/hold.py:7 |
| Owner | com |
| Proposed GT | library / com |

**Representative expressions:**

- <code>device.touch(x, y, MonkeyDevice.DOWN)</code> -- tests/hold.py:10
- <code>device.touch(x, y, MonkeyDevice.UP)</code> -- tests/hold.py:13

**All bindings (1 unique):**
- <code>tests/hold.py</code> L7: <code>MonkeyRunner.waitForConnection(timeout = 60, deviceId = serialno)</code>

## Group 28: axes -> library/matplotlib (2 records)

| Evidence | static_context |
| Needs human | no (0/2) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>plt.gca()</code> @ report_all_results.py:149 |
| Owner | matplotlib |
| Proposed GT | library / matplotlib |

**Representative expressions:**

- <code>axes.get_xlim()</code> -- report_all_results.py:150
- <code>axes.get_xlim()</code> -- report_all_results.py:150

**All bindings (1 unique):**
- <code>report_all_results.py</code> L149: <code>plt.gca()</code>

## Group 29: (energy_csv['timestamp'] - energy_csv['timestamp'] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>(energy_csv['timestamp'] - energy_csv['timestamp'].shift()).fillna(0)</code> -- report_all_results.py:42


## Group 30: ax.get_xaxis() -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>df_energy[power_feature].plot(color='darkblue',linewidth=1.0)</code> @ report_all_results.py:214 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>ax.get_xaxis().get_offset_text()</code> -- report_all_results.py:236

**All bindings (1 unique):**
- <code>report_all_results.py</code> L214: <code>df_energy[power_feature].plot(color='darkblue',linewidth=1.0)</code>

## Group 31: ax.get_xaxis().get_offset_text() -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>df_energy[power_feature].plot(color='darkblue',linewidth=1.0)</code> @ report_all_results.py:214 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>ax.get_xaxis().get_offset_text().set_visible(False)</code> -- report_all_results.py:236

**All bindings (1 unique):**
- <code>report_all_results.py</code> L214: <code>df_energy[power_feature].plot(color='darkblue',linewidth=1.0)</code>

## Group 32: device -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter device</code> @ tests/test_helper.py:17 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>device.type(text)</code> -- tests/test_helper.py:18

**All bindings (1 unique):**
- <code>tests/test_helper.py</code> L17: <code>parameter device</code>

## Group 33: device -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter device</code> @ tests/test_helper.py:22 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>device.shell('input keyevent KEYCODE_BACK')</code> -- tests/test_helper.py:23

**All bindings (1 unique):**
- <code>tests/test_helper.py</code> L22: <code>parameter device</code>

## Group 34: device -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter device</code> @ tests/test_helper.py:40 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>device.drag(s, e, 500, 20, -1)</code> -- tests/test_helper.py:45

**All bindings (1 unique):**
- <code>tests/test_helper.py</code> L40: <code>parameter device</code>

## Group 35: df['experiment'] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df['experiment'].unique()</code> -- report_all_results.py:102


## Group 36: df[['energy_arm', 'energy_mem', 'energy_g3d', 'energy_kfc']] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df[['energy_arm', 'energy_mem', 'energy_g3d', 'energy_kfc']].sum(axis=1)</code> -- report_all_results.py:92


## Group 37: df[consumption_feature] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df[consumption_feature].plot()</code> -- report_all_results.py:258


## Group 38: df_energy.loc[t1:t2, [power_feature, 'timestamp_delta']] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter df_energy</code> @ report_all_results.py:51 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_energy.loc[t1:t2, [power_feature, 'timestamp_delta']].product(axis=1)</code> -- report_all_results.py:52

**All bindings (1 unique):**
- <code>report_all_results.py</code> L51: <code>parameter df_energy</code>

## Group 39: df_energy.loc[t1:t2, [power_feature, 'timestamp_delta']].product(axis=1) -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter df_energy</code> @ report_all_results.py:51 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_energy.loc[t1:t2, [power_feature, 'timestamp_delta']].product(axis=1).sum()</code> -- report_all_results.py:52

**All bindings (1 unique):**
- <code>report_all_results.py</code> L51: <code>parameter df_energy</code>

## Group 40: df_energy['temperature'] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_energy['temperature'].mean()</code> -- report_all_results.py:66


## Group 41: df_event -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>parameter df_event</code> @ report_all_results.py:212 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_event.iterrows()</code> -- report_all_results.py:215

**All bindings (1 unique):**
- <code>report_all_results.py</code> L212: <code>parameter df_event</code>

## Group 42: df_statistics[['mean_difference', 'cohensd', 'improvement', 'savings_after24h']] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_statistics[['mean_difference', 'cohensd', 'improvement', 'savings_after24h']].to_latex(buf=f)</code> -- report_all_results.py:206


## Group 43: df_statistics[['n', 'sample_mean', 'sample_std', 'mean_difference', 'welchsttest_statistic', 'welchsttest_p', 'cohensd', 'improvement', 'savings_after24h']] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_statistics[['n', 'sample_mean', 'sample_std', 'mean_difference', 'welchsttest_statistic', 'welchs</code> -- report_all_results.py:208


## Group 44: df_statistics[['n', 'sample_mean', 'sample_std']] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_statistics[['n', 'sample_mean', 'sample_std']].to_latex(buf=f)</code> -- report_all_results.py:202


## Group 45: df_statistics[['welchsttest_statistic', 'welchsttest_p']] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>df_statistics[['welchsttest_statistic', 'welchsttest_p']].to_latex(buf=f)</code> -- report_all_results.py:204


## Group 46: energy_csv['timestamp'] -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Proposed GT | ? / ? |

**All expressions:**

- <code>energy_csv['timestamp'].shift()</code> -- report_all_results.py:42


## Group 47: experiment -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>experiment.replace(experiment_pivot+"-lint-", "")</code> @ report_all_results.py:121 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>experiment.replace(experiment_pivot + '-lint-', '')</code> -- report_all_results.py:121

**All bindings (1 unique):**
- <code>report_all_results.py</code> L121: <code>experiment.replace(experiment_pivot+"-lint-", "")</code>

## Group 48: experiment -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>experiment.replace("-", " ").title()</code> @ report_all_results.py:122 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>experiment.replace('-', ' ')</code> -- report_all_results.py:122

**All bindings (1 unique):**
- <code>report_all_results.py</code> L122: <code>experiment.replace("-", " ").title()</code>

## Group 49: experiment.replace('-', ' ') -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>experiment.replace("-", " ").title()</code> @ report_all_results.py:122 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>experiment.replace('-', ' ').title()</code> -- report_all_results.py:122

**All bindings (1 unique):**
- <code>report_all_results.py</code> L122: <code>experiment.replace("-", " ").title()</code>

## Group 50: vc -> ?/? (1 records)

| Evidence | manual_reasoned |
| Needs human | yes (1/1) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>ViewClient(device, serialno, **kwargs2)</code> @ run_cached_script.py:39 |
| Owner | None |
| Proposed GT | ? / ? |

**All expressions:**

- <code>vc.touch(x, y)</code> -- run_cached_script.py:49

**All bindings (1 unique):**
- <code>run_cached_script.py</code> L39: <code>ViewClient(device, serialno, **kwargs2)</code>

## Group 51: ViewClient -> library/com (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>from com.dtmilano.android.viewclient import ViewClient</code> @ tests/writely-pro-view_holder.py:6 |
| Owner | com |
| Proposed GT | library / com |

**Representative expressions:**

- <code>ViewClient.connectToDeviceOrExit(**kwargs1)</code> -- tests/writely-pro-view_holder.py:14

**All bindings (1 unique):**
- <code>tests/writely-pro-view_holder.py</code> L6: <code>from com.dtmilano.android.viewclient import ViewClient</code>

## Group 52: ViewClient -> library/com (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>from com.dtmilano.android.viewclient import ViewClient</code> @ tests/simplegallery.py:6 |
| Owner | com |
| Proposed GT | library / com |

**Representative expressions:**

- <code>ViewClient.connectToDeviceOrExit(**kwargs1)</code> -- tests/simplegallery.py:14

**All bindings (1 unique):**
- <code>tests/simplegallery.py</code> L6: <code>from com.dtmilano.android.viewclient import ViewClient</code>

## Group 53: ViewClient -> library/com (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>from com.dtmilano.android.viewclient import ViewClient</code> @ tests/gnucash.py:6 |
| Owner | com |
| Proposed GT | library / com |

**Representative expressions:**

- <code>ViewClient.connectToDeviceOrExit(**kwargs1)</code> -- tests/gnucash.py:14

**All bindings (1 unique):**
- <code>tests/gnucash.py</code> L6: <code>from com.dtmilano.android.viewclient import ViewClient</code>

## Group 54: ViewClient -> library/com (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>from com.dtmilano.android.viewclient import ViewClient</code> @ tests/acrylicpaint.py:6 |
| Owner | com |
| Proposed GT | library / com |

**Representative expressions:**

- <code>ViewClient.connectToDeviceOrExit(**kwargs1)</code> -- tests/acrylicpaint.py:14

**All bindings (1 unique):**
- <code>tests/acrylicpaint.py</code> L6: <code>from com.dtmilano.android.viewclient import ViewClient</code>

## Group 55: ViewClient -> library/com (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>from com.dtmilano.android.viewclient import ViewClient</code> @ tests/talalarmo.py:6 |
| Owner | com |
| Proposed GT | library / com |

**Representative expressions:**

- <code>ViewClient.connectToDeviceOrExit(**kwargs1)</code> -- tests/talalarmo.py:14

**All bindings (1 unique):**
- <code>tests/talalarmo.py</code> L6: <code>from com.dtmilano.android.viewclient import ViewClient</code>

## Group 56: ViewClient -> library/com (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>from com.dtmilano.android.viewclient import ViewClient</code> @ tests/uhabits.py:6 |
| Owner | com |
| Proposed GT | library / com |

**Representative expressions:**

- <code>ViewClient.connectToDeviceOrExit(**kwargs1)</code> -- tests/uhabits.py:14

**All bindings (1 unique):**
- <code>tests/uhabits.py</code> L6: <code>from com.dtmilano.android.viewclient import ViewClient</code>

## Group 57: json_data -> library/json (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>json.load(data_file)</code> @ tests/test_helper.py:55 |
| Owner | json |
| Proposed GT | library / json |

**Representative expressions:**

- <code>json_data.update({serialno: interaction})</code> -- tests/test_helper.py:56

**All bindings (1 unique):**
- <code>tests/test_helper.py</code> L55: <code>json.load(data_file)</code>

## Group 58: ax -> library/matplotlib (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>fig.add_subplot(111)</code> @ report_all_results.py:128 |
| Owner | matplotlib |
| Proposed GT | library / matplotlib |

**Representative expressions:**

- <code>ax.set_ylabel('Energy Consumption (J)')</code> -- report_all_results.py:131

**All bindings (1 unique):**
- <code>report_all_results.py</code> L128: <code>fig.add_subplot(111)</code>

## Group 59: fig -> library/matplotlib (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>plt.figure()</code> @ report_all_results.py:127 |
| Owner | matplotlib |
| Proposed GT | library / matplotlib |

**Representative expressions:**

- <code>fig.add_subplot(111)</code> -- report_all_results.py:128

**All bindings (1 unique):**
- <code>report_all_results.py</code> L127: <code>plt.figure()</code>

## Group 60: df.groupby('experiment') -> library/pandas (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>pandas.DataFrame(experiments_report_data)</code> @ report_all_results.py:89 |
| Owner | pandas |
| Proposed GT | library / pandas |

**Representative expressions:**

- <code>df.groupby('experiment').transform(lambda g: replace(g, stds=2))</code> -- report_all_results.py:99

**All bindings (1 unique):**
- <code>report_all_results.py</code> L89: <code>pandas.DataFrame(experiments_report_data)</code>

## Group 61: df_statistics -> library/pandas (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>pandas.DataFrame.from_dict(experiment_statistics, orient='index')</code> @ report_all_results.py:197 |
| Owner | pandas |
| Proposed GT | library / pandas |

**Representative expressions:**

- <code>df_statistics.to_csv(dir_path + '/_%s_statistics.csv' % energy_feature)</code> -- report_all_results.py:198

**All bindings (1 unique):**
- <code>report_all_results.py</code> L197: <code>pandas.DataFrame.from_dict(experiment_statistics, orient='index')</code>

## Group 62: energy_csv -> library/pandas (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | RETURN_PROPAGATION |
| Key binding | <code>pandas.DataFrame(\n        [\n            {\n                'timestamp': k,\n  </code> @ report_all_results.py:28 |
| Owner | pandas |
| Proposed GT | library / pandas |

**Representative expressions:**

- <code>energy_csv.groupby(['timestamp'])</code> -- report_all_results.py:37

**All bindings (1 unique):**
- <code>report_all_results.py</code> L28: <code>pandas.DataFrame(\n        [\n            {\n                'timestamp': k,\n  </code>

## Group 63: experiments_report_data -> python/python (1 records)

| Evidence | static_context |
| Needs human | no (0/1) |
| Reason | LOCAL_DEFINITION |
| Key binding | <code>[]</code> @ report_all_results.py:85 |
| Owner | None |
| Proposed GT | python / python |

**Representative expressions:**

- <code>experiments_report_data.append(experiment_report_data)</code> -- report_all_results.py:88

**All bindings (1 unique):**
- <code>report_all_results.py</code> L85: <code>[]</code>

## Group 64: sm.graphics -> library/statsmodels (1 records)

| Evidence | static_obvious |
| Needs human | no (0/1) |
| Reason | TRANSITIVE_IMPORT |
| Key binding | <code>import statsmodels.api</code> @ report_all_results.py:11 |
| Owner | statsmodels |
| Proposed GT | library / statsmodels |

**Representative expressions:**

- <code>sm.graphics.violinplot(consumption_by_experiment, ax=ax, labels=experiment_labels, plot_opts={'cutof</code> -- report_all_results.py:129

**All bindings (1 unique):**
- <code>report_all_results.py</code> L11: <code>import statsmodels.api</code>
