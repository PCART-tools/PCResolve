[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "pcresolve"
version = "1.0.5"
description = "Project-level static analysis for Python API ownership and library usage provenance"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
license-files = ["LICENSE"]

[project.urls]
Homepage = "https://github.com/PCART-tools/PCResolve"
Repository = "https://github.com/PCART-tools/PCResolve"
Issues = "https://github.com/PCART-tools/PCResolve/issues"

[project.scripts]
pcresolve = "pcresolve.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["pcresolve"]

[tool.pytest.ini_options]
norecursedirs = ["tests/fixtures/tested_projects"]
